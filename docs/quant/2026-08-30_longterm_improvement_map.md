# Long-term improvement map (2026-08-30)

Ranked improvement map for liquiditybot, synthesized from five measured mining passes (backlog/docket, math, public data, asset universe, infra) — all provenance read in-repo or from named public sources this session.
Never-assumed rule: every item cites a repo measurement, docket id, file:line, or [PUBLIC:source]; an idea with no provenance was dropped, not padded (see the closing section).
Fences: **SAFE** = measurement/reports/tests/bug fixes that do not alter which orders are placed or how they fill — ships freely. **BOUNDARY** = cohort-resetting, operator ARM only. **FREEZE** = model-side, 2026-08-10 adjudication only. **VENUE LAW** is immovable (Kraken sole execution, withdrawals impossible, dry_run defaults true).
Era-6 has been accruing since 2026-08-30T15:32:36Z; nothing here reads the accruing gate as a trend, and every BOUNDARY item rides a named adjudication.
Asset universe in one line: Kraken offers 638 online USD pairs and ~23 crypto pairs at T2 quality, but the recommended net addition is **ZERO** — the 12-slot REST envelope is full, the alt tail is measured net-negative, and additions deflate era-6 effective n (details below).
The vault owed-measurements register remains the exhaustive queue; this map is its ranked head (40 items), and unlisted register items stay owed, not cancelled.

## NOW (SAFE — ship without ceremony)

1. **OpenD revival (moomoo feature trio)** — `config.json:1142` has `opend_path=""` so pc_supervisor never relaunches OpenD; port 11111 probed DOWN 2026-08-30T21:26Z while moomoo-api 10.08.6808 is installed. One config value revives three degraded features: `equity_risk_z` (32.4% neutral) and `opt_pcr_z`/`opt_oi_pcr_z` (32.8% neutral) — the single biggest measured win in the public-data pass, zero code change. First step: set `moomoo.opend_path` + headless auto-login (documented at `config.json:1143`), verify `moomoo.available=true` in status.json and check the US options-chain entitlement (if unentitled, the options pair stays honestly neutral and no code fix exists).
2. **TRIALS-1 residual: run the archetype null battery on the PC** — the ledger + battery SHIPPED; the only residual is the operator executing it so OF-5 leaves its self-labeled "assumed N" world. Provenance: `docs/superpowers/plans/2026-08-27-archetype-null-battery.md` (addendum) + HANDOFF rows. First step: follow the addendum instructions verbatim and file the printout.
3. **ALERT-DRIFT: reconcile the alert plane** — cloud holds exactly 2 rules (read via `/api/prometheus/grafana/api/v1/rules`, HANDOFF 08-30 entry) while the repo's Brier-degraded and drift-stuck YAMLs in `docs/grafana/` were never provisioned, and the manipulation rule exists ONLY in cloud, unversioned, flapping Normal→Pending ~2x/day. First step: export the manip rule to YAML and commit, provision the two dead YAMLs, verify all four via the rules API; then pull 14d of the underlying series and propose a flap-margin fix from the measured Pending-excursion distribution, not taste.
4. **Pusher file-contention hardening** — 603 `gc_log_offset.tmp` WinError-32 incidents in `gc_log_pusher.log` (08-29/30) plus one status.json Permission-denied (08-30 12:00:12) are the largest measured error class in the telemetry plane (HANDOFF:392-394; `scripts/gc_log_pusher.py:25,57` — the .tmp is the atomic-replace staging file). First step: grep the log for the exact colliding lines, wrap the `os.replace` in bounded retry-with-jitter + per-attempt unique temp names, and add an injection test that plants a held handle and watches the retry fire.
5. **CTRL-2: consume the control-arm tag** — the tag is WRITTEN (merged at cut #8) but nothing reads it; an era-current baseline arm from minority-arm rows is the ONLY route out of the universal CONFOUNDED_BASELINE verdict on every gate code (sandbox report §5; `scripts/gate_efficacy_report.py:228-237` two-vocabulary verdict fn; same item as vault owed #104). First step: poll for the first live tagged row, then build the comparator to clear ERA_OVERLAP_MAJORITY by construction; needs ~n=30 usable minority-arm rows of accrual.
6. **OWED-101: pre-register the majors-vs-alts split** — WHY-1 measured the alt tail at -$3.67 net over 27 trips vs BTC/ETH/LINK +$4.56 over 18 (`docs/quant/2026-08-26_why_losing_deep_dive.md`). Era-6 already began 08-30, so first step is to check whether the registration window was met; if not, draft the registration doc now targeting the NEXT boundary — a registration written after peeking is worthless.
7. **Fee-truth re-derivation batch at Tier-3 22/38** — POWER-1 (MinTRL: "62 trades at configured fees, 1,603-or-infinite at Kraken T1") and POWER-2 (n_eff=9.92 at n=33, cost tolerance 43bps) were computed in the pre-cut-#9 fee world and need re-derivation at real fees (`docs/quant/2026-08-22_gate_power_analysis_mintrl.md`, `..._walkforward_resampling_tranche2.md`); OWED-97 (cost-stack close, refuters R1-R5 unrun) and OWED-7 (fourth cost number) re-anchor the same way. Includes the concrete fix for OWED-98: the struck 16/26 schedule hard-coded at `scripts/cost_attribution.py:76-77` with an inverted conservatism comment — third recurrence of the-method #1. First step: fix cost_attribution to the real tier, then re-run the power analysis at 22/38.
8. **QT-1 re-baseline + FEE-3 venue tier verification** — quant_trials `TIER_CFG est_fee_bps=40` vs deployed 38 (`scripts/quant_trials.py:73-82`): drift narrowed to 2bps at cut #9 but the conscious re-baseline adjudication is still owed — never flip it inside a passing commit (owed #106). FEE-3: OM-080 fee reconciliation has fired n=0 times ever (`execution/order_manager.py:767`); the operator's app screenshot established Tier 3 by eye, the API read is still owed — one read-only TradeVolume call, needs the first real credential, prefer post-readout.
9. **Cluster bootstrap + BCa in the resampling lab** — WF-2b's sqrt(n/n_eff) SE inflation is a patch bolted onto a bootstrap the tranche-2 doc says "structurally cannot see" overlap (`docs/quant/2026-08-22_walkforward_resampling_tranche2.md:48,64-70`; Politis-White block length 1.3 proved serial dependence is not the problem — concurrency is); and `scripts/walkforward_lab.py:281-289` uses plain percentile CIs on a series measured at skew +1.25, kurtosis 4.51. First step: resample connected components of the trip-overlap interval graph (de Prado sequential-bootstrap equivalent), print beside WF-2/WF-2b, report component count as the honest n; add a BCa path with a coverage check on a skew-matched synthetic null.
10. **Design-effect Wilson CI on effective n** — `scripts/cohort_eval.py:130-143` runs wilson(k,n) on raw n while `cohort_effective_n` (:484-534) computes AFML uniqueness n_eff report-only; its own docstring concedes SE scales as 1/sqrt(effective_n), and reading-discipline (e) demands deflations hit liked claims too. First step: print a companion line `wilson(k*n_eff/n, n_eff)` next to the registered one — gate, bands, and selection rule untouched (the registration is the law; this is a second route, not a retune).
11. **Anytime-valid confidence sequence, report-only** — a time-uniform CS (Howard-Ramdas e-process) makes reading the accruing cohort statistically legal instead of forbidden peeking, and answers power and stopping in one object where the MinTRL doc had to ask post-hoc whether the registered fixed-n=50 gate was even powered. First step: emit the running sub-Gaussian CS for mean net-per-trip alongside each cohort_eval accrual line; adoption as the era-7 verdict rule is drafted for the operator docket, never self-applied.
12. **MDA/clustered honest feature importance (LS-1)** — `ml/models.py:594-710` importance is in-sample split-gain only, and the learning-symmetry synthesis rules the 29/64 dead-feature list "unaudited until the MDA re-derivation runs" (`docs/quant/2026-08-20_learning_symmetry_synthesis.md:18-19,89-90`). First step: `scripts/mda_importance.py` permuting Spearman-clustered features on purged OOF folds reusing `ml/walkforward.py`'s deployed purge basis; report rank agreement and which "dead" features survive. Cutting anything is item 34 (FREEZE).
13. **ALGO-5 evidence pack (the SAFE half)** — two measurements the pre-named boundary adjudication needs: (a) OWED-23b, the `calibrate_fills.py` re-run on the clean 637/637 ledger — blocker gone, re-run simply never happened, and contamination already fed forward into sim parameters (any resulting parameter change is fill-sim = BOUNDARY; the measurement is SAFE); (b) censoring-aware exit-path analysis — the registered "~30 uncensored paths" basis selects against slow losers, since `ml/labeling.py:68-74` time-barrier semantics are exactly a right-censoring mechanism; KM curves with IPCW weights over the fills ledger show how the stop-width/ladder picture shifts with censored trips included.
14. **Turbulence SAFE batch: observability + adjudication evidence** — ship the TURB-1 SAFE-NOW items (turbulence absent from status.json entirely, silent stale-hold, zero config_guard coverage — `2026-08-22_crisis_block_synthesis.md` disposition), then build the evidence pack for item 23: Ledoit-Wolf shadow vs the hardcoded `* 0.05` shrinkage (`regime/correlation.py:327`; rank fidelity decays 0.970→0.899 as window shrinks 250→60), per-asset Mahalanobis contribution shares over the 08-20 blackout (one scalar broadcast at `main.py:6280` sidelined PAXG at vol percentile 0.52), a GPD absolute-floor backtest against the episode history, and an HMM filtered P(crisis) overlay (lag-1 autocorr +0.988 is persistence information deployed hysteresis-bypass throws away, `regime/macro_regime.py:424`). All shadow/report — no wiring.
15. **Unify FC independence weight with corpus uniqueness** — `ml/foundational_confidence.py:39-41,141-146` accepts any caller-chosen independence u in (0,1] while `ml/corpus.py:200+` already ships the canonical effective_n/uniqueness (relocated 2026-08-29 as the one home for the semantics). First step: helper deriving u from corpus uniqueness for a trip's bar span, plus a pin test that FC and gate_truth_report produce identical uniqueness on a shared fixture. FC is a shadow instrument — no decision path touched.
16. **ATTR-1 / sent_fear: measure the never-fired feature** — fear_spike has fired 0 times corpus-wide (battery print 08-30); live read 21:25Z: score 0.03, crowd source 0.0, 4/6 figures 0.0, fetchers alive (`ml/features.py:126,368,393`; `sentiment/scanner.py:287-292`; thresholds `config.json:884` + scanner.py:155). First step: histogram score/vol_z over the corpus to establish the reachable range (SAFE), revive any dead-air source (crowd 0.0 suspect — bug-fix class). Threshold recalibration is explicitly NOT pure bug-fix: it shifts a model feature and fear_filter risk shading — adjudication required.
17. **Diode fresh-worktree CI leg** — Smart App Control leaves the C++ referee PERMANENTLY 8-skip on the target box (HANDOFF:444) though it agrees with Python at 1e-9; the only cross-implementation check on fill/accrual accounting currently runs never (owed #105 names the fresh-worktree leg as where it belongs). First step: GitHub Actions job (ubuntu, g++) compiling `diode/lb_diode.cpp` in a fresh worktree against a committed fixture corpus, wired advisory-first per auto_update's HARD/ADVISORY split (`auto_update.py:285-298`).
18. **auto_update locally-born-deploy restart gap** — a deploy committed on the PC itself reads "local is AHEAD … runner untouched" and never restarts the runner, so the box runs stale code stamped current until the next remote update (HANDOFF:83-84, measured 08-30; outcome table ~`scripts/auto_update.py:188-203`). First step: confirm the outcome mapping, then either explicit restart-request on the local commit path or an operator-visible "restart owed" flag in pc_status — keeping fast-forward-only and the diverged-wedge rule intact; test by planting a local-ahead worktree.
19. **Discharge-verification sweep** — five register items may already be discharged by later work and must be verified before anyone rebuilds them: OWED-3 (null-model floor vs the 08-27 archetype battery), OWED-55 (drawdown gauge vs the twice-rebuilt boards), OWED-74 (cohort contamination — era-4 closed at n=54; transfers to era-6 or moot?), OWED-76 (gate_efficacy intervals vs the 08-26/27 effective-n Wilson upgrade), OWED-90 (dead-man path vs PAGER-1's verified-silent window). Each check is a grep/read; close or re-scope each in the register the same session.

## NEXT BOUNDARY BATCH (operator ARM, one era reset)

20. **SWEEP-1 + SWEEP-1b: the fail-open pair** — CRITICAL: the margin-health veto (`risk/leverage.py:75`) takes the no-constraint path on a 0.0/failed TradeBalance read — full ladder to 10x on the exact fetch failure it exists to survive, injection-reconfirmed 08-29; sibling: book-staleness (`core/watchdog.py:140`) reads never-delivered books as age 0 = fresh, injection-confirmed. First step is SAFE and immediate: land the waiting xfail pins in `tests/test_fail_open_pins.py`; the real fixes (separating dry-run-unknown from live-fetch-failed) ride this ARM at the top of the batch.
21. **SWEEP-0: hedge force-close coordination** — CRITICAL: `derisk_actions` can force-close a HEDGE with zero hedge coordination — no cooldown arm, no FW-070 — violating the durable hedge-shape rule (unwinds ungated, re-opens evidence-gated, cf454d5). Provenance: `docs/quant/2026-08-20_codebase_sweep_docket.md`. First step: injection test proving the uncoordinated close, then wire the close through the existing hedge-churn guard path.
22. **ALGO-5 + GB-1 (pre-named next adjudication)** — stop widths + time-decay ladder, EXCLUDED from cut #9 because its net-CI spans zero on fills alone and needs the candle re-sim first (CLAUDE.md moratorium pre-naming; `docs/quant/2026-08-29_tail_control_sim_{registration,results}.md`; vault owed #67, law ee7a94a2); GB-1 re-assesses `give_back.arm_gain_pct=0.6` against the NEW 82bps break-even buffer, not cut-8's 166bps. Evidence arrives from item 13; first step: candle re-sim, then the adjudication packet.
23. **Crisis-block fix: TURB-1 + REG-8 v2** — the deployed crisis trigger is DEFECTIVE: fires ~7% by construction on stationary noise (rank threshold on its own history), measures co-movement atypicality not stress, and one scalar blacks out the whole book (`docs/quant/2026-08-22_turbulence_instrument_verification.md`); REG-8 v2 is the adjudicated shape — DELETE the turbulence clause (predicates 2→1), route turbulence → governor shrinkage and breadth+stress → RiskProtocolStack, zero new gates (`2026-08-22_REG8_crisis_predicate_algorithm.md`). Item 14 delivers the evidence pack; the dissolution itself resets the cohort.
24. **OWED-1: the money-path levers decision (+CFG-B)** — the operator decision still owed on levers ranked above horizon extension: maker-only execution, asymmetric entry banding, pooling across ~50 symbols, and exit geometry — the binding lever per payoff-asymmetry (vault owed #1; concepts/payoff-asymmetry). Ride the CFG-B config keys (fee stack, use_margin) docketed BOUNDARY by the 08-28 full config audit (commit cd84c2aa) into the same sitting. This decision sets what the rest of the batch contains — schedule it before, not after, the ALGO-5 packet is opened.
25. **SWEEP-3/5/8** — watchdog PNL-velocity input, CVaR buffer lookup, and fast_cycle fetch-loop defects from the codebase sweep (`docs/quant/2026-08-20_codebase_sweep_docket.md`). Batch them into the same ARM as items 20-21 — one era reset, not three.
26. **Fill-sim recalibration cluster** — OWED-13c/XV-022 (fill-probability functional form misspecified; shipped 0.048 is the conservative end of [0.048,0.082]), OWED-40b/XV-023 (per-TTL recalibration, declared at `core/fill_calibration.py:17`), OWED-57b (passive_base_prob conditional on no-cross), OWED-60 (sim accepted 31 dust legs down to 1.17e-09 ETH), OWED-61 (MP-7 queue gating inert — `queue_aware:true` protects nothing, explicitly moratorium-fenced). All fill-sim = one boundary; item 13's clean re-run feeds the parameters.
27. **CONC-1: concurrency/uniqueness** — mean uniqueness 0.301 means the cohort buys information at ~1/3 nominal rate (`2026-08-26_why_losing_deep_dive.md`); raising it is a sizing/concurrency decision pre-named for the boundary AFTER ALGO-5. Do not act before readout; the map lists it so nobody "fixes" it early.
28. **REG-7: regime taxonomy vs measured occupancy** — retire extinct bull_volatile, split range, rename bear→drift_down, per the pre-registration `docs/quant/2026-08-20_REG7_taxonomy_occupancy_prereg.md`. Low urgency; batch with the crisis-block adjudication since both touch regime routing.
29. **REG-6 (rewritten scope) + CTRL-1 learner** — the momentum-sign discriminator is FALSIFIED (crisis_down longs 91.3% > crisis_up 78.4%) and veto efficacy reads CONFOUNDED_BASELINE/PARTIAL_OVERLAP across every code — direction unresolved, not refuted; the cure is CTRL-2 accrual (item 5). CTRL-1's shadow gate-weight learner sandbox (`sandbox/control-arm-shadow-weights` @ 11eafb97+f0f3c370) needs REBASE+RETEST (base stale); the learner half is cohort-resetting, operator-only. First step: rebase+retest the sandbox while CTRL-2 accrues; adjudicate both when the baseline exists.
30. **LS-2: Bayesian uncertainty-aware sizing** — point-estimate Kelly systematically over-bets under estimation error; `risk/position_sizer.py:96-195` uses a flat 0.25 haircut, not uncertainty scaling, on 343 live labels at uniqueness 0.152 (learning-symmetry synthesis: "exactly what this needs"; math pass: Beta posterior on n_eff-deflated counts, Baker-McHale shrinkage). First step is SAFE: replay the fills ledger computing posterior-Kelly f* per ticket beside the shipped f*, report the size-ratio distribution and which SZ-030 marginal tickets a shrunk bar declines — input to the sizing adjudication, no live change.
31. **ws_feed enable decision (Binance.US depth push)** — venue-law-LEGAL (read-only data; `data/ws_feed.py` cites invariant 3 itself, staleness-gated, ships DISABLED at `config.json:975` with the geo-block reason at :974), and it targets the one latency lever the 08-28 audit left open (83-86% of REST latency is venue-side). But fresher books change which orders are placed: the flag flip is cohort-resetting. First step (SAFE): run the feed in shadow consumed only by a logger for a week; measure staleness-gate hit rate and REST-vs-WS divergence; the enable goes to the operator with those numbers.

## FREEZE ADJUDICATION (model-side, operator decision)

32. **New data as model inputs: ATTR-2 + OWED-45 + feed wiring** — liquidation/OI awareness is absent and the context calendar knows only scheduled events (`2026-08-20_event_record_surge_outlier.md`); the institutional/government data queue (family 45a-45f) is PROPOSED (vault owed #45); and wiring ANY revived or new feed (sentiment, DVOL, cross-venue OI, GDELT) into the model is freeze-gated even where the feed itself is SAFE telemetry. First step: assemble one adjudication package post-era-6-readout ranking candidates by the telemetry evidence items 16/36/37 will have produced.
33. **Venn-Abers calibration** — `ml/calibration.py` PAV isotonic + 5-bin ECE feeds Kelly probabilities literally (its own header) and isotonic overfits step boundaries at small n; VA predictors carry distribution-free validity and the [p0,p1] width is an honest calibration-uncertainty gauge (Brier instability documented in `2026-08-19_brier_spike_diagnosis.md`). First step (SAFE): fit VA and isotonic on the same purged OOF ledger, compare Brier/reliability, report the width distribution; swapping the deployed calibrator waits on the freeze.
34. **Dead-feature prune decision** — after item 12's MDA run, cutting any of the 29/64 candidates is a model-side operator decision (LS-1's freeze half). Listed so the measurement and the decision are never conflated.
35. **The frozen model-side queue** — OWED-10 (clean same-hyperparameter monotone-constraint comparison), OWED-12 (missing-data indicator columns, gated on the liveness diagnostic), OWED-14 (sequential bootstrap, gated on a bagged family winning selection on merit). Recorded so they are not re-derived; untouched until the freeze lifts.

## RESEARCH (measure first, no commitment)

36. **Cross-venue funding/OI + Deribit DVOL telemetry** — the genuinely missing public series per the data pass: OKX `/api/v5/public/open-interest`, Bybit v5 OI+funding history, Deribit funding/OI and the DVOL implied-vol index — all keyless [PUBLIC: exchange docs, as-of Jan-2026 cutoff, NOT re-verified live; the Pyth reversal (went keyed 2026-08-26) is exactly why verification comes first]. OKX funding is ALREADY wired (`data/okx_feed.py:169`) — only OI and DVOL are new. First step: verify each endpoint + rate limit live, then wire DVOL as context telemetry (SAFE); model input is item 32.
37. **GDELT DOC 2.0 news-volume/tone** — the one geopolitics-shaped source with grounding, and the grounding is thin: keyless but harshly and opaquely rate-limited (throttle says 1 req/5s; a 2026-07 field measurement found ~60 req/90min triggered a long block) [PUBLIC: blog.gdeltproject.org; github.com/alex9smith/gdelt-doc-api#22]. Viable only at ≥15-min cadence with 1-2 queries, and its incremental value over the wired RSS+HN machinery is unmeasured. First step: offline pull over a known event window, measure incremental information vs sent_fear inputs before wiring anything.
38. **OWED-20: volatility estimator replacement question** — measured touch rates run ~50x the Gaussian implication (vault owed #20). Research whether the estimator should be replaced; no commitment — any replacement that changes sizing or barriers is boundary/freeze downstream.
39. **Fill-truth instrumentation** — OWED-63: raw websocket book capture, the ONLY instrument that can measure intra-poll fill bias (registered, not started); OWED-8: fill-hazard time consistency, where 366/366 tapes were excluded, zero fitted, and ~3.5x sim optimism remains unmeasured — closes on engine-cadence recordings covering the timeout horizon. Both are measurement infrastructure for the fill-sim cluster (item 26).
40. **Where the edge actually lives** — OWED-102: the conviction edge is directional only (n=5 nets +1.46%/trip at true fees) — power it before believing it; OWED-51: the free population has never been searched (adjudication input, no owner). Both feed the item-24 levers decision; neither is actionable until measured.

## Asset universe

Kraken snapshot 2026-08-30T21:24:04Z (AssetPairs + Ticker, all 638 online USD-quote pairs returned): **T1** (24h quote vol ≥ $5M, spread ≤ 10bps) = 13 pairs, 9 crypto-only; **T2** (≥ $1M, ≤ 25bps) = 27 pairs, 23 crypto-only; **T3** (≥ $250k, ≤ 50bps) = 70. Current book: 12 assets = 7 config `trading_pairs` + 5 live skimmer promotions (`outputs/skimmer_active.json`, dry-run merge only). Three measured caps say data availability is NOT the binding constraint:

- **Hard envelope**: the REST-fallback budget (3 req/s, `config.json:1127` `skimmer._max_extra_doc`) caps the universe at ~12 pairs; base(7)+extra(6)=13 starves the book poll and the config guard FATALs on exactly this. The 12-slot envelope is full today.
- **WHY-1 alt tail**: DOGE/ARB/LTC/ADA/SUI lost -$3.67 net on 27 trips while BTC/ETH/LINK made +$4.56 on 18; median ticket $18.00 sits at/below the fee floor — widening the universe multiplies fee-floor tuition, not edge (`2026-08-26_why_losing_deep_dive.md:41-42,60`).
- **CONC-1 deflation**: mean uniqueness 0.301, effective_n 9.92 of 33 (SE ×1.82); added assets raise concurrency and make every era-6 readout slower and weaker.

**Recommended envelope: 0 net additions.** If the operator later raises the REST envelope AND tickets clear the fee floor (post-ALGO-5), the data-quality ceiling is ~23 T2 crypto pairs — rotated through the existing skimmer machinery, never hardcoded. Any `trading_pairs`/skimmer-candidate change is BOUNDARY (entry universe, cohort-resetting, operator ARM only). One-day volume snapshot; a stability claim needs a multi-day scan (not run).

## What this map refuses to include

Dropped for failing the never-assumed rule or crossing invariants: **Pyth Hermes** (no longer keyless — Core upgrade 2026-08-26 requires an API key, verified at docs.pyth.network); **Google Trends** (no keyless official API; pytrends is ToS-violating scraping); **Binance.com futures data** (US geo-blocked; Binance.US is spot-only by design, `binanceus_feed.py:10-13`); **Cboe delayed JSON** (the site's internal feed, licensing unclear — not demonstrably 100%-public); **US Treasury par curve / Stooq / new daily equity endpoints** (redundant with wired FRED DFF/T10Y2Y/VIXCLS or contractless — the honest verdict is that keyless-legal equity data is daily-only, and the repo's real intraday equity read is the moomoo basket item 1 revives); **Hawkes fill-arrival modeling and a CPCV upgrade** (the repo already measured constant hazard adequate and already runs CSCV with the deployed purge — no deficiency to fix); **any asset-universe expansion now** (contradicted by three repo measurements above); and categorically **anything touching dry_run, withdrawal capability, or non-Kraken execution** (hard invariants). Also trimmed for rank, provenance intact and queued in their sources: glass-console phone push and cycle I/O timing telemetry (INFRA pass), the OWED-27 per-gate conversion counter, and the vault register's long tail of minor SAFE items. Limits of this map: it ran no new measurements — every rank leans on the miners' cited measurements as-of their stamps (live values 2026-08-30T21:24-27Z; HANDOFF read 08-30); all fee-anchored numbers older than cut #9 are flagged for re-derivation, and the four [PUBLIC: as-of-cutoff] rate limits in items 36-37 must be re-verified before any wiring.
