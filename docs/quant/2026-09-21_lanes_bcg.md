# Lanes B/C/G land (2026-09-21, SAFE plane)

Operator directive 2026-09-21 14:59Z: "Finish the parked lanes." Built
in-session (no subagents), all SAFE class: measurement/report tooling only.
No cohort-resetting surface touched; EXEC_ERA `12-10d4d0c2` unchanged.

## Lane B — dataset intake seam

`scripts/intake_dataset.py` + `tests/test_intake_dataset.py` (11 tests) +
DI family registered in `core/codes.py` (DI-000 verified; DI-010 manifest
invalid; DI-020 payload missing; DI-030 hash mismatch; DI-040 rows
mismatch; DI-050 schema mismatch).

- Inbox: `research/corpus/inbox/`, pairs of `<name>.manifest.json` +
  payload (default sibling `<name>.csv|parquet`, overridable via
  `payload_file`). Manifest spec as posted on the session bus 2026-09-21.
- Verified per drop: payload exists, sha256 == content_sha256, counted
  rows == manifest rows, declared columns ⊆ payload columns.
  NOT verified (stated in every DI-000 receipt): dtype fidelity,
  time_range content, gap/dupe claims — deeper profiling is a later lane.
- Every verdict — pass or fail — is appended to the hash-chained
  `receipts.jsonl` (same construction as core/audit.py:_h). A torn receipt
  chain refuses (`INTAKE_RECEIPTS_TORN`) before any append. Failures exit
  2 after ALL drops are processed — one bad drop never hides another's
  verdict.
- Parquet payloads count via the lazy optional duckdb seam (refusal
  `INTAKE_DUCKDB_MISSING`, never traceback). Verify + receipt only: no
  payload is modified, moved, or promoted into any engine-visible path.

## Lane C — boundary evidence payload

`scripts/boundary_payload.py` + `tests/test_boundary_payload.py` (4 tests).
One JSON file for the 09-22 sitting: era_readout (via its documented
`--json` subprocess interface), gradeability census (no doc write),
reject-inference bounds, gate ecology, quant_db second-engine crosscheck.
Every section recorded ok / refused / error — a failed instrument lands in
the payload as a marked hole, never silently dropped. Exit 0 only when
complete. Live run 2026-09-21T20:15Z: all 5 sections ok →
`outputs/boundary_payload.json` (gitignored operational artifact).

## Lane G — verdict-level shuffle null

`scripts/gate_shuffle_replay.py` + `tests/test_gate_shuffle_replay.py`
(7 tests). OF-2/TH-012 discipline applied to the Lane F section-3 verdict.
Statistic: Δ = absorb_rate(elevated|extreme) − absorb_rate(low|normal),
percentage points, EN-000 tick absorbs joined to cross-asset max vol
percentile. Null: B label permutations, tick counts stay paired (within-
tick heterogeneity preserved), p = (1+hits)/(B+1) with a stated 95%
interval on p itself.

**Live reading (2026-09-21, since cut-#12 floor, B=2000, seed 20260921):**
217 joined ticks, 0 corpus gaps. **Observed Δ = −0.716pp; p = 0.586**
(CI95 on p [0.565, 0.608]). The "absorbs cluster in high-vol regimes"
verdict is NOT distinguishable from label-exchangeable chance at era-9
sample sizes — the observed coupling is weakly NEGATIVE if anything.
Advisory only; changes nothing.

## Cross-session integration (live, same push)

The dark-pool session (`conv-eb3b19b7`) conformed to the attach convention
within the hour: `research/corpus/external_dbs.json` now registers their
`darkpool_v2.duckdb` → quant_db exposes
`ext_darkpool_ats_venue_weekly` = **479,291 rows** READ_ONLY (their v2
mirror, up from 321,942). End-to-end verified by `scripts/quant_db.py`
live run. test_quant_db's hermeticity fix (explicit absent-registry path
in fixtures) keeps the suite independent of the live registry.
