# Learning symmetry — common→expert ladder vs this bot (synthesis)

*2026-08-20. 12-agent adversarially-verified workflow (2 recall, 3 ladder,
1 symmetry judge, 3 path generators, 3 refuters; 731k tokens, 0 errors).
Full machine-readable result: `2026-08-20_learning_symmetry_result.json`
(matrix rows with evidence, complete path texts, refuter verdicts). Every
claim below survived its refuter or is marked otherwise.*

## The matrix in one paragraph

28 mechanisms across the common/professional/expert ladder: this bot is
**AHEAD on 16** (the entire López-de-Prado professional stack — triple
barrier, purged walk-forward, uniqueness weighting, PBO/DSR, calibrated
Kelly — plus honesty machinery no tier of the literature carries:
pre-registered outcome gates, counterfactual veto corpus, purge-mutation
self-probes, the referee lattice), **AT 2, BEHIND 2, ABSENT 8** — and of
everything missing, only **2 rows both matter and are reachable**:
honest feature importance (MDA/clustered — the 29/64 dead-feature list
rests on a ranking whose method is unverified) and Bayesian
uncertainty-aware sizing (exactly what 333 live labels at uniqueness
0.152 calls for; spec-on-paper now, boundary-#5 ship). 7 rows are
BLOCKED_BY_DATA at ~3 closes/day — no architecture fixes that. 10 rows
DON'T MATTER here (grid/DCA/HFT classes at a 36h directional book).
Symmetry cuts both ways, and the judge said it plainly: the honesty
machinery **exceeds what the signal underneath can currently pay for**.

## Cleanup verdicts (the "aspects that don't matter")

- SAFE_NOW: TH-015 code defined-never-emitted (registry hygiene);
  n_eff standardization across ~12 panel routes that still quote SE on
  nominal n (fix, not remove).
- FROZEN_DOCKET: the 29/64 feature prune (schema change under freeze AND
  its own importance method is unverified — measure before cutting);
  MP-7 inert queue gating (single-axis discipline); probe/conviction
  split consumers (entry behavior = boundary).
- KEEP (verified worth their cost): Kish ESS, era-mix TVD, bracket
  divergence recording, multi-horizon shadow, registry lineage,
  postmortem mitigations.

## Improvement paths that SURVIVED adversarial refutation

Pre-readout (SAFE, registration-only — run these while accrual finishes):
1. **2D conviction kill-test** — regress realized gross on conviction
   score over the 333 live labels; ρ≥0.69 needed for fewer-larger to
   clear the wedge. One offline read decides whether the signed
   COST_BOUND action has fuel. *Cheapest decisive measurement on the books.*
2. **Fee-truth band census** (owed-88 companion) — count admitted trips
   whose EV margin < the 40/80 understatement; deterministic, double-
   derived. Quantifies the bleed boundary #5 removes.
3. **Maker fill-intensity** — 30-day ledger window (~465 attempts) gives
   SE 0.014 on fill probability; the 48h read (3/31) is worthless
   (Wilson [0.03, 0.25]). Defends the 0.8pp/trip maker-vs-taker gap.
4. **Signal-class S1–S4 offline** (edge-training design, already
   adjudicated SAFE) — the fatter-gross class (multi-day, sd ~8%) is the
   only path where the wedge falls to 10–20% of one sd; breakeven 55–60%
   hit vs today's impossible 42.9% at a 22.6–29.1% actual.
5. **Null path** (the honest baseline): trading to readout costs ≈ −$28
   to −$50 expected and buys the pre-registered verdict — the cheapest
   information purchase available. Every other path is measured against
   this one.

KILLED by refuters (and why, so they stay dead until the data exists):
- Horizon h864 extension — `horizon_shadow.csv` holds no h864 pairs; the
  paired design has no data. Revives only via registered shadow labeling.
- Post-FOMC/CPI drift — the calendar file holds 8 events; n≈40–60 was
  fiction. Event-class hypotheses need an event-history backfill first.
- Capital scale-up as edge — proportional fees make it sizing honesty
  only; post-positive-verdict item.

## Blind spots that survived (the "unwilling to look past" list)

Four propositions stand with evidence and named deciding measurements —
including: the cohort is 93% probe admissions (the gate may partially
measure the exploration policy; decided by stratifying the readout by
admission class — registered in the signed table's contamination
acceptance), and live-label convergence arithmetic at ~3/day (n=1000 live
labels ≈ a year; decided by the S1–S4 offline reuse of the 11k candidate
corpus instead). Two were killed: the $800-non-portability universal
(percentage statistics are capital-invariant) and the
apparatus-eats-attention claim's strong form (the attention mix is real;
the causal claim about hypothesis design failed re-derivation).

## Standing corrections this pass minted

- PSI drift has DIRECT retrain authority (`ml/monitor.py:491` →
  request_retrain) — earlier census reach-tags calling it alarm-only are
  corrected; the alarm-never-autotunes law holds for THRESHOLDS, while
  retrain requests are its designed output.
- The dead-feature list's importance METHOD is unverified — treat the
  29/64 as unaudited until the MDA re-derivation runs.
