# 47 remaining findings verified by execution — the register

**Date:** 2026-09-07
**Method:** 25-agent workflow (`wf_b1d423d4-e8b`, 2.78M tokens, 900 tool
uses): 37 hunt findings (the 4 with truncated verdicts on 09-05 + all 33
medium/low) and all 10 candle findings, each reproduced against the RUNNING
system by one agent and adversarially reviewed by another instructed to
refute, then fence-classified. Agents were forbidden from writing to the
repo. HEAD at synthesis `8183a3a4`.
**Outcome:** 36 CONFIRMED+SAFE · 9 CONFIRMED+BOUNDARY · ~14 REFUTED (several
of them the verifiers' own earlier verdicts) · 4 ALREADY_FIXED at HEAD ·
16 CANNOT_DETERMINE with what settles each.
**Acted on this session (batch 3, SAFE):** h7, h5, h44, h41-residual,
h31/h36, h18, h19 — see `tests/test_verified_findings_batch3.py`. Everything
else below is docketed WITH its executed evidence; nothing here is a claim
from reading.
**Raw:** the adjudication text follows verbatim.

---

HEAD at synthesis `8183a3a4` (raw `git rev-parse`; reviews ran across b638da67→4bb6f3ae→b6ab541a→8183a3a4, every batch diffed its anchors across the move — none moved). Indices are 0-based throughout (h = hunt_findings.json, c = candle); the "1-based #52 orphan parts" caveat is moot — that finding is h51, verified in batch 44/45/49/51. Batch text for h10/h21/h24/h31/h56 arrived truncated: verdicts intact, fences for h10/h24 are mine [I]. Two ALREADY_FIXED commits below were named by me via raw `git log -S` (a541afe7, 5f26d3f3/1cda4264); everything else is transcribed from executed evidence in the batches.

## 1. CONFIRMED + SAFE (blast × likelihood, descending)

1. **h7 ARM LIVE gate has no point-of-use pin.** Sole gating read `main.py:6883`; submit sites `main.py:1524/2536/3051/5018/5774/5941` behind gate calls `:1591/:3011/:4534/:5268`; `OrderManager` has no `arm` attr. Evidence: `-p neuter_arm_gate` (gate≡True) → 0 of 2027 pytest tests red; `-p block_arm_gate` → 15 red (gate is on the path); `test_deny_live_not_armed_blocks_adds` vacuous — blocked by LB-010 ceiling `risk/long_book.py:748`, not the gate; only real pin `scripts/smoke_test.py:1471-1479` (outside pytest). Fix: pytest pin with a stub ladder holding a live rung; optional `submit()` predicate defaulting True with exits exempt (without the exemption = BOUNDARY). Pin: `dry_run=False`, live rung, `live_armed=False` → `orders.calls==[]`, `live_armed=True` → non-empty; must go red under neuter (today nothing does).
2. **h5 fill_ledger width guard one-directional — wound live.** `core/fill_ledger.py:305-306` adopts file header only if ⊆ COLS else writes 18 cols. Live `outputs/fills.csv` (2026-09-07T00:16:40Z): header 17, widths {17:1239, 16:6}, 6 rows `exec_era=None` (lines 1038-1043, silently excluded from stamp-purity accrual, no counter), `book` None ×1245, 4 fills appended after `39f36e49` (2026-09-05T14:42Z) at width 17 — every future fill loses `book` until a runner-stopped migration (`migrate rc 0 → 1245 rows @18`). Fix: pad/truncate to file header width + migration + excluded-row counter. Pin: 17-col writer into 18-col file → uniform widths; today `[18,18,18,17] RAGGED True`.
3. **h16 cost tools pool three fee schedules; none reads `exec_era`.** `grep -c exec_era`=0 in `scripts/fee_anatomy_report.py`, `cost_attribution.py`, `fee_reprice.py`, `cost_truth_report.py` (control `cohort_eval.py`=12); `fee_reprice.py` argparse only `--fills/--maker-bps/--taker-bps`; `cost_truth_report` pools 664 OM-000 legs bps {0:4,22:33,25:104,38:37,40:475,80:9}, 0/1305 records carry `exec_era`; labels `cost_attribution.py:276/283` frozen "configured (25/40)". Evidence: mean_fees_pct FULL 0.7137 / ERA9 0.6169 / BLANK 0.7217 (double-derived). Fix: `--era` on the fills tools; audit→fills join for cost_truth; labels from `_booked_fees()`. Pin: two-era fixture `--era 9-16ec821e` excludes era-8 rows; today argparse rejects the flag.
4. **h63 C++ diode harness dark on this box + blind to cut logic.** `tests/test_cpp_diode.py` → 8 skipped (no compiler on PATH; `C:\Program Files\LLVM\bin\clang++.exe` clang 22.1.8 present; `.superpowers/sdd/progress.md:39` step never executed). Fixture `:98-123` has no trip closing in `[1786359815, 1786403127)`. Evidence: PATH+LLVM → 8 passed; M1 `ERA4_MIN_N 50→49` red; M2 `CAPITAL_EPOCH_TS→B4_TS` green; M3 `lb_diode.cpp:486 std::max→std::min` green; live fills (00:15:38Z) head.exe 99 = py `era4_trips` 99, m2/m3 → 105 (Δ6 real trips on the pre-registered count). Fix: LLVM on PATH + fixture trip (close ts ~1786380000) asserted EXCLUDED; constants text pin never alone. Pin: the fixture trip → red under M2 and M3.
5. **h44 deploy-gate bandit scope < DoD law.** `scripts/auto_update.py:461-462` `-r core data execution ml risk api` (82 files/26k LOC) vs law `-r . -x ./.venv,./tests` (195/67k); `tests/test_dod_gates.py:28-37` pins names only. Evidence: B602 ×7 planted → gate 2 findings, law 14 (+`main.py`, `runner.py`, `regime/`, `sentiment/`, `strategies/`, `scripts/remote_control.py`); law runtime 8 s vs wall 900 s. Fix: law argv. Pin: gate argv covers `main.py`/`runner.py`/`scripts`; today red.
6. **h41 residual: red HARD gate `continue`d on a "cannot find" last line.** `scripts/auto_update.py:572-575`. Evidence (real `_run_gate`): `assert_with_cannot_find_last_line rc=1 missing=True`, `pytest_style_summary_last_line rc=1 missing=True` → deploy admitted; `repo_module_missing/plain_red/hang` → False; classifier mutants M1-M4 all killed (pins cover the classifier, not the HARD-gate skip). Reachability narrow: 0 hits `cannot find|is not recognized` in shipped scope; a library `FileNotFoundError: [WinError 2]…cannot find the file` last line is the path. Fix: never `continue` a HARD gate (advisory only, or rc∈{127,9009}). Pin: HARD rc=1 + "cannot find" → refused; today admitted.
7. **h13 three fee-fallback vintages on dry-run/bare-config boots; stale comments.** `config_guard.py:3723 if fatals and not dry_run: raise` — this box is permanently dry (sentinel) so `enforce(no fee keys, dry_run=True)` RETURNS with one CRITICAL and consumers diverge: `execution/pretrade.py:85` 40/80, `execution/order_manager.py:189` 40/80, `risk/position_sizer.py:139` 0.65 (25/40), `main.py:879` (25,40), `scripts/assurance_check.py:105` 40/80, `config_guard.py:2568-2569` 0.0 (new in 4bb6f3ae). Comments `pretrade.py:80-84`/`order_manager.py:183-188` wrong on fee (20/35), floor (`_venue_worst_row()`=25/40 at `config_guard.py:71`) and mechanism: inject 30/50 → WARN either `allow_sub_floor`; inject 40/80 → WARN "not a published Kraken tier". Fix: one accessor = `_venue_worst_row()`, delete per-module literals, rewrite comments. Pin: bare-config `PretradeGate`/`OrderManager`/`PositionSizer` fee tuples all == `_venue_worst_row()`; today 40/80≠25/40.
8. **h19 naive local timestamps in report provenance.** 15 naive `datetime.now()`/`fromtimestamp` sites across 9 `scripts/*.py`; `scripts/markout_report.py:793` naive beside `:758` aware in one JSON; `outputs_gc.py:195,210`. Evidence: UTC 00:13:58Z → `ledger_continuity --no-append` "read at 2026-09-06T19:13:59", `kyle_lambda --json` same; `CAPITAL_EPOCH_TS` prints 18:05:27 vs 23:05:27Z; skew −5 h DST/−6 h std (`time.timezone=+21600`). Fix: `timezone.utc` everywhere. Pin: every `read_at` parses tz-aware; today naive.
9. **h18 cost_attribution ratio literal + struck labels.** `scripts/cost_attribution.py:352` `65.0/(KRAKEN_T1_MAKER+TAKER)` prints ×1.182 while `_booked_fees()`=(20,35); labels `:276/:277/:279/:280/:349-350` literal "25/40","40/80","40/40","15/15". Evidence: 65.0→9999.0 → 37/37 relevant tests green; S8 pin `tests/test_verified_findings_batch2.py:128` covers `KRAKEN_T1_*` only. Fix: ratio = `sum(_booked_fees())/sum(T1)`, labels from numbers. Pin: shipped-config ratio == 55/55 = 1.000; today 1.182.
10. **h31+h36 supervisor self-handoff clobbers its own log; exit-forensics line destroyed.** `scripts/pc_supervisor.py:280-284` child_log = own `LOG_PATH` (`open(...,"a")` never closed), `:927 _spawn([PY, str(_SELF)])` own_log default True, `log()` `:170-178` prints AND appends, `_arm_exit_forensics` `:1104-1113` (c10ddf5c). Evidence: `os.lseek` shows inherited pointer lags EOF by a constant → each `print` overwrites prior appends; production log (7547 lines, 19:22:01 local): `process exit (pid` 0 across 23 post-08-16 handoffs, 29 torn fragments (live: L7545 written by current pid 15412), 53 exact dups, `changed on disk` ~2× inflated; injection under pythonw: exit line PRESENT t=1.5 s, GONE t=5.5 s (2/2); control `own_log=False` survives. Fix: `own_log=False` on `:927` only. Pin: self-handoff + atexit line survives successor's first print; today lost. Existing pins spawn `runner.py` only (`test_child_log_rotation.py:112/115`, `test_pc_supervisor_guard.py:188/201`) or monkeypatch `_spawn` (`test_pc_supervisor_lock.py:165/187`).
11. **h34 gc_log_pusher `_save_state` fixed tmp/no retry/no cleanup.** `scripts/gc_log_pusher.py:72` `open(tmp,"w")`, `tmp = path+".tmp"`. Evidence: 2 threads×400 → `{32:227, None:252, 5:138}` + `.gc_log_offset.tmp` litter, same batch POSTed 3×; production log (19:24:03): `WinError 32` 454 attributable/529 upper, `WinError 5` 2, `[Errno 13] …gc_log_offset.tmp` ×75 (the fixed name failing at the open). Fix: `core.persistence.atomic_write_json` (INVALID 0, litter [] in the same test). Pin: 2×400 concurrent → 0 errors 0 litter; today red.
12. **h62 `tests/test_book_tag.py:109` negative control vacuous under `pytest tests/`** (prior REFUTED overturned). Root cause `tests/test_label_ret_persistence.py:28` module-level `logging.disable(logging.WARNING)` collected before test_book_tag runs; `ml/history.py:1140` warning never records. Evidence: planted ML-013 on accepted row → full collection `1 passed`; `--ignore` the contaminant → red. Fix: `caplog.at_level(logging.WARNING, logger="liquiditybot.ml.history")` (pytest 9.1.1 `_force_enable_logging` only via `set_level`/`at_level`) + scope the disable to a fixture. Pin: v62 injection under default collection → red after fix. Only dead negative control in suite (`test_liquidity_tier_isolation.py:128,171` protected).
13. **h61 PT-061 pinned by source text.** `tests/test_close_reason_audit.py` reads `main.py`; dead-coding `main.py:1757` (`if False: get_audit().log(`) → 3 passed; comment rename → test ERRORS. Anchors `main.py:1736/1738/1751/1757/1768`; no behavioural PT-061 test (only hand-written records `test_hedge_sim.py:146,252`). Fix: finalize a position, assert PT-061 record with verbatim close_reason (MUT-C collapse already caught by payload pin). Pin: red under MUT-B; today green.
14. **h53 state.json 3.35 MB/~30 s, 97% candidates, ∝ horizon².** `runner.py:1694` unconditional on change; `core/persistence.py:695-714` fsync+rename; residency = horizon `ml/history.py:2599-2602`, bars `:2466`. Evidence: candidates 3,354,518 B (97.3%), 15×2160×4 arrays; cadence 30.0–31.6 s (+67.6 s stall) → 8.5–9.9 GB/day; 1256 cands span 35.92 h = horizon 36.0 h. Fix: write-on-change / hot-cold split with ML-085 eviction (`:2628-2636`) measured before/after. Pin: unchanged state → second `snapshot()` skipped; today written.
15. **h43 OM-080 audited on mismatch only; instrument still reports planted 40/80.** `execution/order_manager.py:888` inside `if mismatched:`, `:904` terminal `except: log.debug`; `core/session_digest.py:244-268` filters `== OM_FEE_RECON_MISMATCH`. Evidence: status.json `fee_recon=None` (no creds, `kraken_feed.py:238-246`); audit.jsonl 84,967 lines → exactly 1 OM-080 (line 69769, 2026-08-29T15:47:07Z, 40/80 vs 25/40); `read_om080_fee_recon → (40.0,80.0,1,1)`; `tests/test_fee_reconciliation.py:117` pins ok→no audit. Fix: registered OK code in `core/codes.py`, emit on ok, handle in `_load_om080_records` same commit. Pin: ok → OK record present; today absent.
16. **h4 control_arm unit = (asset, hour); no cell-n helper.** `ml/history.py:457-460`; anchors `:1089` (live `time.time()`) vs `:2856` (`bar_time`); `control_arm_effective_n` 0 hits. Evidence (signal_history 00:16:38Z, 26,082 rows): 7425 tagged/315 arm, 2404 cells/102 arm, `mixed_cells 0`, rows/armcell mean 3.09 max 12; Wilson rows (0.0381,0.0473) vs cells (0.0351,0.0512); first row-n comparator overstates precision ~1.76×. Fix: `control_arm_effective_n` (distinct cells) + reader-trap doc `:345-397`; re-anchoring live rows needs an arm-generation stamp. Pin: helper returns 2404 on fixture; today ImportError.
17. **h20 ExitPolicy bare defaults + false parity comment; est_fee_bps unpinned.** `ml/labeling.py:111` "matches the live engine's CODE default" false since b638da67 (`risk/profit_tiers.py:245` → `ProfitTierEngine({}).est_fee_bps==40.0` vs `ExitPolicy().est_fee_bps==0.0`); 8 diffs bare vs `from_config`. Evidence: M1 `from_config` est_fee_bps→7.0 → `tests/test_p35_label_sim_parity.py` 16 passed; M2 `ts_max_bars+1` → red. Production constructors `main.py:1007`, `scripts/train_meta.py:256` both `from_config`; `ml/history.py:2810` skips sim when None. Fix: extend parity pin (est_fee_bps/gb_*/chandelier_*/be_*), fix comment. Pin: M1 red; today green.
18. **h45 `_run_gate` tail drops VACUOUS/SYNTHETIC/ARMED.** `scripts/auto_update.py` tail = last line; `:478-481` logs `corpus=<path>` (where it looked). Evidence (`LB_OUTPUTS` popped): assurance tail `'50 passed, 0 failed'` with 1 VACUOUS line in blob; overfit tail = stray `INFO:…ML-076` line, `7 ARMED checks passed` and `corpus: SYNTHETIC benchmark (loaded rows=0 < 640)` dropped. Fix: capture `VACUOUS|SYNTHETIC|ARMED|passed` lines. Pin: corpus-free run → tail contains "SYNTHETIC"; today False. Only `test_dod_gates.py:125` pins tail.
19. **h42 pusher-bounce marker written on failed bounce.** `scripts/auto_update.py` `_ensure_pushers_current`. Evidence: stubbed powershell rc=1 → `marker='8183a3a4'` + "pushers bounced" logged, next cadence 0 calls; `-ErrorAction SilentlyContinue`/none → rc=0 at PID 4, `-ErrorAction Stop` → rc=1. Fix: `-ErrorAction Stop` + marker only on rc==0. Pin: rc=1 → marker unchanged; today written.
20. **h35 `cycle_duration_max_sec` resets per process.** `runner.py:1122-1123`, `:1256-1258`; `core/persistence.py` `cycle_dur` 0 refs (`:481` writes `cycle_lifetime` only, `:1032` restores). Evidence: status.json `cycle=1524`, `cycle_lifetime=856007`, `cycle_duration_max_sec=63.18`; ≥9 `runner starting:` in current log generation. Fix: ADD `cycle_duration_max_lifetime_sec` (extend-only, write-path). Pin: snapshot→restore preserves max; today lost.
21. **h26 gate_1 `min_pool_size_usd` default 0 → unconditional PASS; guard blind (dormant).** `strategies/signal_gates.py:120-121`. Evidence: key popped → pool=1000 True, `{}` True, nan True, `'abc'` ValueError at gate time; `config_guard.validate` on popped/=0/=-1/='abc'/gate_1 deleted/whole `signal_gates` deleted → findings=3 new=0 every time; shipped 500000. Fix: `inf` default or MISCONFIGURED code + guard FATAL. Pin: absent key → not PASS / validate adds a finding; today red.
22. **h25 `buckets_agree()` vacuous True (dormant).** `core/fill_calibration.py:169-170`; `scripts/calibrate_fills.py:266,270`; JSON has no bands-compared key. Evidence: 0/1/1+3-DEFERRED bands → True, no XV-022; mutation (None) → `tests/test_fill_calibration.py:139` red (suite pins the vacuous branch); live `fill_calibration.json` non-vacuous; no reader beyond writer. Fix: None + `bands_compared`; flip `:139-140`. Pin: <2 bands → not True.
23. **h52 recording knobs unguarded; 0 = off.** `config_guard.py` 0 hits; `data/recording.py:60,194-195` sentinels; `runner.py:244-246` negative `max_file_mb` passes through; non-numeric `retain_*` swallowed `runner.py:1507-1514`. Evidence: `{0,0,0}`/`{-5,-1,-1}`/`{'abc','x',None}`/`{}` → validate delta 0; `SinkRotator(max=0)` 10 MB single file; `prune(0,0)` 0 of 5; live 6.9 GB, 0.698 GB/day. Fix: FATAL both arms ≤0, WARN non-numeric/negative (single-arm 0 documented + pinned `tests/test_recording.py:51,64,126`). Pin: both-zero → finding.
24. **h51 orphan `.partNN` invisible to prune; pruner makes them.** `data/recording.py:117`, `:197-199` base-first unlink, caller `runner.py:1501-1514`. Evidence: orphan parts → discover []/prune []; held handle on part002 → base deleted, part orphaned, return claims session; live 0 orphans/7.27 GB. Fix: discover by stem, parts first, honest return. Pin: orphans pruned; today [].
25. **h37 corpus_sync no lock (latent).** `scripts/corpus_sync.py` no `SingleInstanceLock` (:17/:84 comments), loser rename dies `:199-202`; spawner `pc_supervisor.py:848`, `:448-462`, `:594-634`. Evidence: race → 11 rows/6 distinct; `outputs/corpus_sync.log:779-793` 15 timeouts in 170 s on 2026-08-14 (≥10 concurrent); live corpus 26097 rows 0 dups. Fix: lock. Pin: race → 6/0.
26. **h33 gc_log_pusher partial-key state → KeyError wedge, supervisor ALIVE (latent).** `scripts/gc_log_pusher.py:173,:257`; `pc_supervisor._fresh :210` mtime-only. Evidence: `{"inode":123}` → KeyError ×5, pushed [], `_fresh True`; schema unchanged since `7710cf8a`; production 0 hits. Fix: validate keys → default; failure counter to supervisor. Pin: `_load_state({"inode":123})` → default.
27. **h55 `_adopt_tail` double full-trail walk at boot.** `core/audit.py:136-142`, called `:110` and `:244`. Evidence: 84,923 lines/55.55 d, 2 walks after first `log()` (43/42 ms); dict-vs-set ~1%; live verify `ok False`, 16 seams, `tamper False`. Fix: tail-seek only; do NOT window `seen` (`:352/:382`, changes seam/tamper classification). Pin: one walk per `log()`; today 2.
28. **h54 `poll()` list scans.** `ml/history.py:2628,:2638` per cycle from `main.py:4468`. Evidence: 36.3 ms vs dict 3.0 ms, identical output. Fix: per-asset `t→idx` dict. Pin: equality only; no non-flaky red pin exists.
29. **h49 stale prose.** `core/config_guard.py:118-131` calls the cross-lineage recipe "exact"; `:1776-1777` FATAL cites 2026-08-16 while the constant block `:132-139` is the 2026-09-06 per-lineage 43.7/h (naive full-corpus 421.4/h is a pre-fix artefact). Fix: prose only; constant/cap untouched. Pin: n/a.
30. **h56 log-once sets unbounded.** `risk/profit_tiers.py:502,:650,:736` (self-cap only `:837-838`; discard `:742`); engines cached for bot life `main.py:1461-1475,:908`; all four features enabled in shipped config. Evidence: N=5000 → 5000/5000/5000/900. Fix: evict on close. Pin: key absent after close.
31. **h60 five fills fixtures at 17 cols.** `tests/test_cohort_homogeneity.py:22` (+stale comment `:20-21`), `test_fee_anatomy_report.py:16`, `test_hedge_sim.py:30`, `test_ledger_continuity.py:39`, `test_reconcile_weekly.py:35`; `book` since 39f36e49; 0 fills-ledger readers of `book` at HEAD. Pin: header == `core.fill_ledger.COLS`.
32. **h9 force_dry admission reads `bot.dry_run` alone (latent, not constructible).** `runner.py:724-725`, seal `:748-750`, sole `orders.dry_run` write `:750`, sole `self.orders=` `main.py:875`. Evidence: divergent state → "already dry-run", `orders.dry_run` stays False. Fix: admit on any unsafe flag; `tests/test_audit_runner_state.py:341-346` tolerates flag writes, not sentinel. Pin: divergent → both flipped.
33. **c8 mtf_align reads forming daily bar — doc caveat.** `data/kraken_feed.py:422-427` `include_forming=True`, `main.py:6311-6322` → `:4619/:5744`. Evidence: 6.9% end-of-day flips (715/10350, proxy); replay reproducible (`data/replay.py:26`); divergence = live vs offline re-derivation from committed daily bars. Fix: doc. Pin: n/a (optional invariant pin on `include_forming`).
34. **c6 middle-bar coverage claimed, never read.** `strategies/smc.py:196` docstring, `docs/SMC.md:75` vs `:204-215`. Evidence: middle@500 still detects. Fix: prose. Pin: keep middle@500-detects test (guards a BOUNDARY change dressed as a fix).
35. **c9 `SMCEngine.status()` no production caller.** `main.py:1054`; tests only (`test_smc.py:241,271`, `test_visibility.py:50`); `runner.py:1338` → `gc_pusher.py:788-792`; degraded-line 0 in events.jsonl/runner.log full-file. Fix: delete or wire. Pin: n/a.
36. **Incidental (verification-surfaced).** `tests/test_telemetry_restart_coupling.py:61/68/77` call real `_ensure_pushers_current()` with only `OUT` patched → real `Stop-Process` on live pushers under any full-suite run here (grep [K], incident [I]) — patch `subprocess.run`. `tests/test_fee_reconciliation.py::test_unexpected_exception_from_feed_never_propagates` order-dependent flake in full suite (passes alone/whole-file).

## 2. CONFIRMED + BOUNDARY (sign-off required)

- **h21 `market_maker.min_half_spread_bps=26` binds silently on liquid low-vol quotes.** `execution/market_maker.py:109-118`; quote → `main.py:4774-4782` → `plan_entry` → entry_price; 26 unchanged across 0084c16d/db22acfa/59bdcf87/b638da67 while fees went 25/40→40/80→22/38→20/35; `config_guard` 0 hits. Evidence: fee20 σ~0 → half 26.0, QT-010 NOT logged; `min=0` → 21.0 logged; binds iff `sigma_bar_pct<0.03963` liquid only; 964/26082=3.7% signal rows (upper bound; entry clamp rate UNKNOWN). Fence: any value change BOUNDARY (entry price). SAFE piece: lift-with-identical-default 26.0 + guard WARN; pin: validate(shipped) emits it, today 0.
- **h10 `purpose` string sole key for five exemptions.** `execution/order_manager.py:933-951` (market gate `:945`); risk_firewall rate `:202-215`, dupe `:226-237`, no-ref `:349-353`, no-equity `:360-364` [file I]. Evidence (live OM `dry_run=False`, `position_id="NO-SUCH-POSITION"`): `purpose='exit'` → market sell placed, entry/hedge REFUSED; exit past 2× rate budget allowed (FW-021), never dupe-vetoed (FW-030), no-ref/no-equity allowed. Sketch: bind exemptions to a resolvable position reference — BOUNDARY (order lifecycle; invariant 5 exits-always-allowed makes any refusal path sign-off). SAFE piece: registered audit code on exit-with-unknown-position. Fence [I] (batch truncated).
- **h24 NaN mark passes `filter_mark`; equity NaN; drawdown 0.0; hard stop silent.** Evidence: `filter_mark(nan/±inf)→(x,True)`, `total_equity(nan)=nan`, `drawdown_mtm_pct=0.0`, `hard_stop_triggered(nan)→False` no log; reachability: `safe_float`→0.0, `get_tickers` drops NaN, `get_ticker_price`→None, book NaN dropped by `clean_book` + `main.py:2684 mid>0`; but `data/ws_feed.py:90 px<=0` admits NaN → `get_mark` nan (ws ships disabled); mark writers `main.py:2664/2736`, `runner.py:942` source `get_tickers`. Currently unreachable. SAFE half: `ws_feed.py:90` → `not (px>0)`, `filter_mark` reject non-finite (pin: `filter_mark(nan)` rejected, today accepted). BOUNDARY half: hard-stop NaN semantics. Fence [I].
- **c0+c1 `fvg_liq_confluence` binary and ≈ null.** `strategies/smc.py:276-280` else-branch ⇒ d>tol ⇒ 0; ±400-ulp edge sweep → {0.0,1.0}; `FEATURE_NAMES[38]`; `ml/calibration.py:148` tie-check, `:143` names SMC flags degenerate. Finder's fix_sketch trips existing pin `test_fvg_liq_confluence_detects_overlap_within_tolerance` (`assert 0.539==1.0`). Null: real 0.4746 vs 0.4641–0.4852 (5 seeds); 946 windows/51 day-clusters → cluster SE 0.023–0.027, |z|≤0.46. Sketch: drop/replace column — BOUNDARY + model freeze (2026-08-10). SAFE: `docs/SMC.md:82-83` + docstring.
- **c2 `fvg_pull` ignores polarity; `FVG.direction` dead.** `smc.py:182/194/211/215`; mutant `direction=None` → 39 passed; bearish gap above → `fvg_pull(long)=0.8154`. Sketch: split magnet/barrier — BOUNDARY + freeze. SAFE: delete field + one SMC.md sentence.
- **c3 POC single-bin allocation.** Same-bin vs proportional 45.0% (POC moves 55%), vs equal-split 31.6% (68%, = finder's 30.8%); straddle full-store mean 2.09/max 24/67.3% (finder's 80% = last-200-bar artefact); 0 zero-volume bars. Sketch: spread/VA — BOUNDARY + freeze. SAFE: SMC.md wording.
- **c4 missingness == neutral, 7/7.** All paired injections collide; `compute(Bad)` NEUTRAL `faults=1`; 3-bar history NEUTRAL `faults=0` (silent); counter `runner.py:1338`. Sketch: availability columns — BOUNDARY + freeze. SAFE: "neutral = censored" doc.
- **c5 `min_gap_pct=0.02` — FVG count ≈ volatility.** Mean 5.68 (n=26392); return-shuffle null ratio 1.27–1.30; 30-bar block null 0.96 (4/15 assets below null). Not a defect; retune BOUNDARY + freeze.
- **c7 `liq_pocket_pull` saturated.** mean 0.7326, p50 0.815, swing dist p50 0.556% vs cap 3.0%; ex-current-bar Δ0.0033 (`strategies/swing_points.py:40-41`, shared with TH-013). `pull_max_pct` move = BOUNDARY/freeze.

## 3. REFUTED

- **h27 as a live defect.** `execution/pretrade.py:170-178` sentinel / `:299` veto exist (malformed bid → `approved=True p_fill=0.45 ev=+197.76` by direct call), but every book writer (`main.py:2677/2755`, `runner.py:947`) is fed by `data/kraken_feed.py:360→368 clean_book` / `data/ws_feed.py:756,:107`; `clean_book` on `[["n/a",5.0]]`/`[[None]]`/nan/`{}` → None → readers' `or {}` → PT-023. Unreachable from any existing feed; any change BOUNDARY, expected no-op.
- **h55(a)** "verify_chain inside the 900 s wall": `scripts/assurance_check.py` has 0 `verify_chain(` calls, verifies a 5-record `%TEMP%` trail (`:42-43,:64-79`); live-trail verifiers are `core/session_digest.py:205` (hourly) and `scripts/session_import.py:176`. **(b)** set-vs-dict ~1%.
- **h49 "permanently broken"**: live restart 2026-09-06 ~22:00Z advanced `_seq` by exactly 1 (`state.json` salts 675cc603 → cf86efe0); only pre-fix restarts inflate.
- **h31/h36 "every line twice"**: constant pointer lag → ~1 torn + ~1 dup per generation; loss needs a post-spawn parent write (the atexit hook is one).
- **h33 trigger path**: torn saves are INVALID JSON (→ default → re-ship), never partial-key dicts; schema single-commit (`7710cf8a`).
- **h63 "harness dead"**: LLVM on PATH → 8 passed, M1 killed; blind window is the real defect.
- **h13 prior "production shielded"**: only `dry_run=False` (`config_guard.py:3723`); this box is permanently dry.
- **h16 prior "cost_truth_report not an era-pooler"**: pools 664 legs across six bps values, 0/1305 era-stamped.
- **h37 prior "overlap not measurable"**: `corpus_sync.log:779-793`.
- **h42 prior evidence-as-reported**: `inj42.py` on disk stubs `subprocess.run` module-wide (fakes `_git`) → marker `''`, ps calls 2/1/1, not the reported `b638da67/0/0`; clean re-injection reproduces the conclusion; "stopped-count required" overstated (`-ErrorAction Stop` → rc=1).
- **h62 prior REFUTED** = false refute (counter-run `tests/test_[ab]*.py` never collects `test_label_ret_persistence.py:28`).
- **h10 "four" exemptions** → five (equity omitted). **h7 "1 read of live_armed"** → `runner.py:1208` too (cosmetic). **h45 [I] tail** → measured stray INFO line. **h53 pool∝horizon [I]** → [K]. **h35 "88.1 s proves reset"** → predates gauge. **h34 "529 post-POST"** → 454 attributable/529 upper; also fires on junk-advance `:207` and `_drain_rotated :154/:160`.
- **c5 "~half is chance"**: sigma artefact (0.15% < median 0.196%). **c3 prior "69%/30.8% from sampling"**: allocation model, not sampling (only the 80% straddle is). **c8 prior "replay-reproducibility"**: replay serves recorded frames (`data/replay.py:26`). **c1 prior "n_eff=946"**: 51 day-clusters. **c4 "6/7"** → 7/7.

## 4. ALREADY_FIXED at HEAD

- **h41** `_classify_missing_tool` — `1e2996f4` (2026-09-05 23:04:42 −0500); residual §1.6.
- **h49** restore re-mint — `1e2996f4` (`ml/history.py:2998-3002`, `_SALTED_CAND_ID :2335`); residual prose §1.29.
- **h13** absent fee keys FATAL on LIVE boots — `a541afe7` (2026-09-05 12:47:24 −0500, `config_guard.py:3723`); dry-run/bare-config residual §1.7.
- **h20** production never uses bare `ExitPolicy` defaults (both constructors `from_config`; ts_max_bars parity pin real since `5f26d3f3` 2026-07-23); comment `ml/labeling.py:111` from `1cda4264` (2026-07-29) made false by `b638da67`; residual §1.17.

## 5. CANNOT_DETERMINE

- **h60** `tests/test_era4_gate.py:65` 16-col fixture intent — settles: `git log -L65,+3` / author note.
- **h5** whether the deployed binary was `book`-aware at the 4 post-39f36e49 fills — settles: `outputs/auto_update.log` deploy revs vs fill ts (ledger identical either way).
- **h21** entry-time clamp rate — settles: persist quote half-spread + liquidity label per entry (SAFE telemetry), count.
- **h26** GateStats-ledger contamination by gate_1 passes — settles: ledger query for gate_1 PASS with pool<500000 (expected 0; key present in shipped config).
- **h24** whether any mark writer beyond `get_tickers` can carry NaN (batch cut at `main.py:2686` book-mid) — settles: finish the writer walk.
- **h10** verifier's own fence (truncated) — settles: full batch output; BOUNDARY assumed here.
- **h35** restart count 2026-08-07→08-25 (`runner.log.1/.2` unscanned) — settles: scan rotations; ≥9 is a lower bound.
- **h4** live-side hour-straddle rate (1/29 on 37 live rows) — settles: more live-tagged rows or `bar_time` re-anchor.
- **h53** frequency of >60 s snapshot gaps (one 67.6 s in 3.7 min) — settles: full-day mtime series.
- **h56** distinct positions per runner lifetime — settles: fills per `runner starting:` generation.
- **h7** flake root cause (`test_unexpected_exception_from_feed_never_propagates`) — settles: order bisect.
- **h55** live trail `ok:False` 16 seams `tamper:False` — settles: seam list vs known restarts (operator glance).
- **h63** unmutated `era4_trips` logic (hedge exclusion, stale-leg provenance, since-ordering) — settles: extend mutants. **h44** bandit runtime on the deploy PC — settles: one run there.
- **c1** label skill of confluence (temporal null only) — settles: resolution/direction decomposition, day-block CIs. **c3** true POC — no tick data; both allocations are counterfactuals. **c8** whether persisted `mtf_align` at candidate time used the forming bar (`main.py:4628` path not walked) — settles: walk it. **c5** block-null boundary re-chaining bias (≤4 artificial gaps/window, biases against the finding) — settles: masked-boundary null.

Not done here: no wiki consult, no new measurement beyond two `git log -S` calls — this is a synthesis of the batches' executed evidence; volatile numbers (fills.csv, state.json, logs) are as-of the stamps quoted in each item.