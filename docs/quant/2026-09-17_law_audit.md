# LAW AUDIT — CLAUDE.md verified against the code it governs

- date: 2026-09-17 (local) · auditor: Kimi Code session (operator-directed; motive: the law is
  Claude-authored and the operator does not trust it on its face)
- class: SAFE — read-only plus pre-approved battery commands; no engine/config/outputs writes;
  the running bot (DRY_RUN, era 12-10d4d0c2) was not touched
- method: every load-bearing claim re-derived against HEAD (06799f4c) — code read at cited
  sites, numbers recomputed with the repo's own helpers, battery re-run. Verdicts:
  VERIFIED / STALE / WRONG / NOT-RE-DERIVED. Nothing below is a recall from the law itself.

## Headline

**The law is substantially accurate.** Every hard invariant is real, current, and — in two
cases — *stronger* than the law states. The defects found are: one line-drifted citation, one
imprecise sentence (test fixtures), two stale numbers, one measurement written into law whose
derivation could not be located, and a battery instrumentation failure that was **mine**, not
the repo's (see §5). Separately, the vault has two stale registers (§6).

## 1. Hard invariants (CLAUDE.md "never weaken")

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 1 | dry_run defaults true; four-step road to live incl. the force_dry.on sentinel; sentinel fails SAFE-and-silently | **VERIFIED** | `runner.py:1850-1906` (`apply_force_dry_sentinel`): forces `sys_cfg["dry_run"]=True` only, "can never set dry_run False" (docstring, verbatim); boot call at `runner.py:1932` precedes engine construction; the "sentinel is a no-op this boot" warning branch exists verbatim. Sentinel present on disk (mtime 2026-09-16). "Present since 2026-08-01": verified via HANDOFF's 09-16 firing audit — "dry_run true with the sentinel present on all 482 session starts" (secondary citation of a measured audit). |
| 1-precision | "No code path, config default, test fixture, or control command may set it false at runtime" | **IMPRECISE** | No engine/config/control path sets False — confirmed by grep. BUT `scripts/smoke_test.py:1491,1508,2028` set `bot.dry_run=False` / `dry_run=False` deliberately ("pretend live for the gate check"). The SAFE harness is plainly the intended exemption, but as written the sentence forbids it. Proposed reword (§7, D2). |
| 2 | force_dry one-way; flips BOTH bot.dry_run and bot.orders.dry_run | **VERIFIED** | `runner.py:765-766` flips both, with the OrderManager-caches-the-flag comment; direction one-way by construction (sentinel only forces True). |
| 3 | Kraken sole venue via DENY-LIST in main.py ~:861; raises VN_ROGUE_EXECUTION; unwraps _feed; execution_eligible NOT on the path | **VERIFIED** (citation drift) | Deny-list block `main.py:858-880`, raise at `:871` (law says ~:861 — drift). Unwraps `_feed` (:866), deny-list of OKX/BinanceUS/Moomoo/WebData/Context/ccxt (:868-873), inline comment carries the full 2026-09-05 cut-#10 history exactly as the law tells it. `execution_eligible`: zero hits in main.py / runner.py / execution/order_manager.py. Pinned test green (§5). |
| 4 | Withdrawals impossible; deny-list blocks before any network I/O | **VERIFIED — stronger than stated** | `data/kraken_feed.py:66-110`: `FORBIDDEN_PRIVATE_ENDPOINTS` (7 endpoints, law names 4 — the shipped set is a superset) + case/whitespace-normalized matching + path-separator refusal (09-05 hardening: 56/56 respellings, documented in the file's own comment). Membership test at `_private_post:253` fires BEFORE `_throttle()`, path build, signing, and the requests call. |
| 5 | Entries limit-only (OM-011); market = exit ladder's final rung; exits NEVER blocked by kill switches | **VERIFIED** | `execution/order_manager.py:927-953`: submit default `ordertype="limit"`; `:945-950` refuses market unless `purpose=="exit"` (code-enforced, not just doctrine). All four `entries_enabled` sites are new-risk paths: `main.py:1596` (algo abort), `:3093` (hedge **OPEN** — unwinds untouched, matching the durable coordination rule), `:4593` (sweep admission `can_enter=False`), `:5343` (gate recompute). Exit pipeline separate: `main.py:2456/:2547` (protective floor/trail/BE run first; overlays "stay senior"). Not pinned by a single dedicated test — structural trace. |
| 6 | Hash-chained audit; registered reason codes; core/codes.py sole registry, no count written | **VERIFIED** | `core/audit.py:63` sha256 chain hash; `core/persistence.py:687,762` snapshot checksum verify; `core/codes.py`: prefix map of 25 families, append-only doctrine in the docstring, ~220 lines, no prefix count anywhere (exactly as the law claims — the drift story about the old enumerated list is also in the file's header). |
| 7 | Public interfaces stable; extend with defaults | **SPOT-ONLY** | Not exhaustively auditable statically. Sampled the claims the law itself stakes on it (smoke doubles, bracket escape hatch "byte-identical to pre-T5" at config :888) — consistent. A full caller×signature sweep is owed if this claim is ever load-bearing. |

## 2. Era-9 / cut-#12 numeric claims (recomputed, not reread)

| Claim | Verdict | Reproduction |
|---|---|---|
| Fees 15/30, est_fee_bps 30, label_round_trip_cost_pct 0.45 | **VERIFIED** | `config.json` live values; `_fee_reading_doc` carries the three-route cut-#12 provenance incl. `venue_fees.binding_row` distances to the cent. |
| Label PT 220→**180**, SL 165→**135** at σ_bar 0.10% | **VERIFIED** | `barrier_geometry(0.001, 0.45, 8, 6, 4.0)` → pt 0.018 / sl 0.0135 (bps 180/135). Cut-#10 world `cost=0.55` → 220/165 exactly as the law's history line states. Floor binds below σ=0.225% — "the normal case" is accurate. |
| "Barrier geometry moves with the cost by construction" | **VERIFIED** | `ml/labeling.py:33-66` pure helper, called by BOTH the candidate labeler and the live bracket engine (config docs :652/:888 say the same); σ floor = `pt_cost_mult·cost/pt_mult`. |
| Derived entry bar 0.6642→**0.6381** | **VERIFIED** | `PositionSizer(cfg...).p_bar_base` on shipped config → **0.6381** exactly. |
| BE/trail floor 76→**66 bps** | **VERIFIED** | `risk/profit_tiers.py:18,232`: floor = `est_fee_bps·2 + be_buffer_bps` = 2×30+6 = 66 (76 at est_fee 35 — consistent). |
| exec_era = `12-10d4d0c2` | **VERIFIED** | `core/fill_ledger.py:116,133` (`EXEC_ERA = "12-10d4d0c2"`); newest `outputs/fills.csv` rows carry the stamp. |
| Read points n=50 lean / n=100 verdict, pre-registered | **VERIFIED** (registration level) | `scripts/era_readout.py` header: "AS REGISTERED (cut-#11 section 3, adopted for era-9)"; `scripts/cohort_eval.py`: `ERA4_MIN_N=50`, three-outcome rule (NO_GROSS_EDGE / COST_BOUND / CONTINUE) at :388-396. The n=100 rule lives in the registration doc — referenced by the instruments ("the n=100 rule's hole (Q9)"), not a code constant. |
| H0 ≈ **−$0.27**/trip at $60 | **STALE** | `era_readout.py` (run 2026-09-18T02:59Z): the registration ADOPTED the era-measured null **−$0.2851** on 09-15; "CLAUDE.md cut-#12 −0.27" is listed as a superseded candidate. Direction and magnitude fine; the quoted figure is one generation old. |
| Long book "holds 2 of the 5 slots at the cut" | **NOT RE-DERIVED** | As-of-cut-time claim; current `outputs/status.json` (3 open positions: ETH, BTC, LINK) carries no lane/book key, so the split cannot be re-derived from live state. HANDOFF's staging read (09-08 23:29Z: 2 long-book among 5) is the only authority. |
| "12 boundaries in 37.9 days, median 2.43-day" + censoring ladder (11.1%→100%) | **NOT RE-DERIVED** | `git log -S 'EXEC_ERA = '` finds only 6fe6d98d (the provenance-column commit) — the pickaxe route does NOT reproduce the count; no 2026-09-16 mint-price measurement doc exists in `docs/quant/`. The number lives only in the law (and possibly the vault). Per the law's own rule ("re-derive, never quote") it should carry its derivation or an owed-measurement entry. The censoring ladder was not recomputed this pass. |

## 3. DoD battery claims

| Claim | Verdict | Evidence |
|---|---|---|
| Battery scripts/tests exist as named | **VERIFIED** | All 10 scripts + 5 test files present. |
| smoke_test = 205 checks | **STALE** | Ran: **passed 220, failed 0**. The 205 figure (README) has been left behind by growth. Law itself doesn't state 205; README does. |
| overfit_check can silently substitute a synthetic corpus | **VERIFIED — did not trigger** | Ran on **live history, 19950 rows** (summary line read, per the law's own instruction). Real corpus. |
| "Four of seven rungs can fail to arm — read the ARMED count" | **VERIFIED, and then some** | Ran: "passed 4, failed 1" with the explicit trailer "4 ARMED checks passed… read the '--' lines". The one FAIL is **OF-5 DSR (conviction-only, n=33, dsr=0.003)** — which ARMED rather than deferring; the era-12 DSR sub-sentinel DEFERRED (<10 trips). Vault index (09-13): "OF-5 DSR red is the operator-SETTLED state." The law's sentence "OF-5 defers below its conviction-trade floor" is true of the era-subset but silently omits the settled red on the pooled conviction sample (§7, D4). |
| pytest suite green | **VERIFIED after fixing the auditor's own instrument** | First run: result line DESTROYED by my own `| tail` (the exact anti-pattern `.claude/hooks/pytest_pipe_guard.py` exists to catch; the repo's lesson of 2026-08-24 re-learned by me), plus a Windows `PermissionError` in pytest's tmp cleanup (`%TEMP%\pytest-of-johnmason`, locked handles — could not be cleared; worked around with `--basetemp`). Re-run, output to file, rc echoed: **67 passed, rc=0** across the 5 pinned files (41.3s). |
| assurance_check / pyright / ruff / bandit / compileall | **NOT RUN** | Per plan — noted as not-run, not green. |

## 4. Apparatus claims

| Claim | Verdict | Evidence |
|---|---|---|
| HANDOFF = STATE, ONBOARDING = MACHINE, both exist | **VERIFIED** | `docs/HANDOFF.md` (1245 lines, freshness contract, register-decay repair record), `docs/ONBOARDING.md`, `docs/ASSURANCE.md` present. (The 09-05 sweep flagged ONBOARDING/ASSURANCE as decayed — out of scope then; still open per HANDOFF itself.) |
| config_guard FATALs incoherent configs | **VERIFIED** | `core/config_guard.py`: 101 FATAL sites; findings-collector at :775; even FATALs rogue venues in `execution.routing.eligible_venues` (:782). |
| Hooks as described | **VERIFIED** | `ruff_on_edit.py` (PostToolUse, exit-2 feeds lint back) and `pytest_pipe_guard.py` (PreToolUse, pipe warning) match the vault log's descriptions; `session-start.sh` no-ops locally (`CLAUDE_CODE_REMOTE` guard, header-commented). |
| Mindset's "seven dated incidents" | **VERIFIED as the original seven** | Register in `vault/wiki/concepts/the-method.md` now runs to **22** recurrences; the law's seven match #1–7 in shape and order. Law does not claim the register stops at seven. |
| era_readout/discard_ledger are report-only | **VERIFIED** | Both ran; both print and exit ("Report-only. No order path was read or touched."). discard_ledger: 85.1% of rows under retired cost — regimes pooled under one label_era name, flagged NOT ESTABLISHED how much is contaminated. |

## 5. Instrument failures during this audit (the method applied to the auditor)

1. **My pytest invocation piped through `tail`** — destroyed the result line and hid the cleanup
   crash; exit code belonged to the filter. Re-ran with output-to-file + echoed rc: green.
   `.claude/hooks/pytest_pipe_guard.py` exists because "a green whose runtime is implausible"
   and pipes mask rc — I am the measured instance for this session.
2. **`barrier_geometry` unit confusion on first call** — I passed σ as 0.10 (fraction) and got
   PT=8000 bps; the shipped convention is σ=0.001 (0.10%). The law's numbers were right; my
   first reproduction was wrong. Re-derived correctly (§2).
3. **Locked `%TEMP%\pytest-of-johnmason`** — Windows handles; un-removable even with rm -rf.
   Worked around with `--basetemp`; root cause (which process holds it) not chased.

## 6. Vault-side staleness found incidentally (not law errors; the vault is its own authority)

- `synthesis/comparability-boundaries` **stops at cut #9** — cuts #10, #11, #12 have no rows,
  while the vault schema calls it "the one authoritative table of ALL cuts." HANDOFF carries them.
- `concepts/the-method.md` frontmatter says the register runs to **THIRTEEN**; the body has **22**.
- HANDOFF OPEN DOCKET (09-16 firing audit) is live drift the law does not yet name: the law never
  mentions the grid ladder, inventory-derisk overlay, watch lane, or circuit breaker; the break-even
  ratchet is real (224/960 states install a different stop), correcting an earlier "fail-conservative"
  phrasing in HANDOFF/profit_tiers comments. "The bot is SAFE and is not running the experiment the
  law describes" (vault log, 09-16) is the accurate one-line summary of this pass too — zero
  hard-invariant breaches found here; the drift is descriptive, not operational.

## 7. Proposed law corrections (approval-gated — CLAUDE.md does not move silently)

- **D1 (line drift):** invariant 3 citation `main.py (~:861)` → `(~:871)`.
- **D2 (precision):** invariant 1 "No code path, config default, test fixture, or control command"
  → "No engine code path, config default, or control command" (the SAFE battery harness
  `scripts/smoke_test.py` deliberately sets dry_run=False at :1491/:1508/:2028 to exercise the
  live path; the sentence as written forbids what the harness must do).
- **D3 (stale number):** H0 "≈ −$0.27 at a $60 ticket" → "≈ −$0.2851 era-measured (adopted
  2026-09-15); re-derive with scripts/era_readout.py" — or drop the figure and keep the command.
- **D4 (settled red omitted):** the OF-5 bullet should note the pooled-conviction DSR is red and
  operator-settled (vault, 09-13); only the era-12 sub-sentinel defers.
- **D5 (unlocatable measurement):** the mint-price figures (12 boundaries / 37.9 days / median
  2.43 / censoring ladder) need either their derivation committed alongside the law or an entry
  in the vault's owed-measurements. Until then they are recall, not evidence — by the law's own rule 10(b).
- **D6 (README, not law):** "205 checks" → 220 (as of this run; or "checks" uncounted).

## 8. Bottom line for the operator

The law deserves more trust than you gave it — every invariant it stakes real money on is present,
current, and code-enforced; two are enforced more strongly than it claims. What it gets wrong is
the class of things this corpus keeps re-learning: numbers written into prose decay (D3, D5),
citations rot (D1), and one sentence over-generalizes (D2). Adopt D1–D6 or file them; the
NOT-RE-DERIVED items in §2 are the real residue.
