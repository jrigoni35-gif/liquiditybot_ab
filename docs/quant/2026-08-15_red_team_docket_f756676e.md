# Red-team docket - commit f756676e (2026-08-15)

RECOVERED 2026-08-15 from a temp workflow artifact after the originating
session was compacted and ended with the docket UNANSWERED. It existed
nowhere in the repo; the temp file it came from is subject to cleanup.
Panel: .claude/workflows/red-team-panel.js, 26 agents, 5 mandated-position
lenses, cross-examined. 35 raised / 4 withdrawn / 16 surviving.

Rule this docket runs under (from the panel README): **the docket is not
closed until every objection has a recorded disposition.** Concession-rate
reading: 0% = the panel is theatre; 100% = the author stopped thinking.

## Docket

Trial artifact note, re-derived at docket time (2026-08-15, worktree `liquidity-bot-isolation-2bd01a`): `git diff HEAD` is **no longer empty** — it is now `pyrightconfig.json +13/-0` (adds an `include` list: core, data, execution, ml, risk, regime, strategies, sentiment, api, main.py, runner.py). That change is uncommitted, unreviewed by any panelist, and touches a DoD gate's config. The substance of every objection below is commit **f756676e** (`CLAUDE.md +32`, `scripts/overfit_check.py +18`, `tests/test_preventive_maintenance.py +21`, 71 insertions / 0 deletions). Answer the docket against f756676e; see OBJ-13 for the framing defect and the pyrightconfig residual.

| # | Severity | Targets | Objection (one line) | What the author must PROVE to CONTEST |
|---|---|---|---|---|
| OBJ-1 | BLOCKING | `scripts/overfit_check.py:1118` | `Measured 2026-08-15: era exclusion left 346 loaded rows` matches no artifact on disk — readings are {0, 347, 347} — and it ships the exact volatile-number-in-a-permanent-file defect the commit says it evicted from CLAUDE.md. | Name an artifact (path + header timestamp) whose reading is 346, or show why a code comment is exempt from the eviction rule the same commit wrote at CLAUDE.md:148-151. |
| OBJ-2 | BLOCKING | `tests/test_preventive_maintenance.py:180`; `scripts/overfit_check.py:704,833,1075,1084,1089` | The pin's message asserts "exactly ONE place may derive `on_synthetic`"; the file derives that predicate **five** times and the assert reads 1 only because it counts the assignment spelling. Claim C3 (defect #1 fixed) is false. | Produce the count. `grep -n 'source.startswith("SYNTHETIC")' scripts/overfit_check.py` — I re-ran it: 704, 833, 1075, 1084, 1089. Contesting requires showing the four inline sites cannot diverge from :704 *and* that the assert message is nonetheless accurate. |
| OBJ-9 | BLOCKING | `tests/test_preventive_maintenance.py:180`, plus sibling pin at `:156-160` | Same pin, second angle: it cannot fail on the duplication already present, and the sibling `test_prior_base_rate_and_judge_share_one_clip` is green on the *pre-fix* `ml/monitor.py` (`git show 523fde68^`). | Run the sibling pin's assertion against `git show 523fde68^:ml/monitor.py`. If it fails there, this limb dies; if it passes, the suite documents an invariant it does not enforce. |
| OBJ-14 | BLOCKING | `tests/test_preventive_maintenance.py:163-181` vs claims C1/C5 | Mutation-survival: swap `if on_synthetic:` at `:1122` for `if source.startswith("SYNTHETIC"):` — the precise duplication the test forbids — and all three asserts stay green. The test never imports, never runs `main()`, never captures stdout. C5 ("now pinned") moved the count, not the property. | Apply the mutation and show a red test. Note the panel's own correction: a monkeypatch+stdout test does **not** catch this mutant (the two expressions are runtime-identical); only an AST check on the guard, or a module-wide `startswith("SYNTHETIC")` count, pins the named property. |
| OBJ-10 | CRITICAL | commit body defect #1 ("reword the reason string and the banner silently stops printing") | Fragility direction is backwards: `reason` is interpolated *inside* the parens at `:265-267`, so no reword can break either predicate; only a **prefix rename** breaks — and it breaks the retained `startswith` form at 5 sites while the banned `in ...upper()` form survives. The banned copy is strictly weaker-failing. | Exhibit one concrete `source` string that breaks `"SYNTHETIC" in str(source).upper()` but not `startswith("SYNTHETIC")`. If none exists, the rationale must be re-recorded as pure duplication, and the CI ban on the safer spelling (`:178`) reconsidered. |
| OBJ-11 | CRITICAL | CLAUDE.md new paragraph, "`gate_truth_report.py` has applied that standard since 2026-07-29"; `scripts/gate_truth_report.py:194` | The cited exemplar filters `label_era == "triple_barrier"` (exact equality) — the retired 96-bar era — reading 5,328 dead rows and **zero** of the 358 deployed `triple_barrier_h432` rows. Recomputed with the report's own `effective_n()` over the deployed era: n_eff 55.1 < SG_MIN_ROWS 100 → XV-042 THIN, not the XV-040 ALIGNED it prints. | Show that `:194`'s literal matches the deployed era under `config.json ml.label_max_bars=432`, or that the era filter is intentional and disclosed. `ml/history.py:645-654` already documents this exact bug class as fixed elsewhere. |
| OBJ-12 | CRITICAL | `scripts/overfit_check.py:254` promoted to `:1121` | On the live branch the new headline prints `live history (N rows)` where N is loaded (candidate+live). `:256-259` says in the author's own words that this conflation "cost a session's diagnosis on 2026-08-08". Census: candidate 10,331 : live 327 = 31.6:1; `cloud-mirror` report shows headline 2141 with n_live < 60 (gbt excluded on `min_live_rows=60` alone) ≥ 35x. | Show the split cannot be printed. `n_live` is unpacked at `:701` and in scope at `:1121`; the split must come from `last_load_stats` (the `:252-253` `source_counts()` fallback is a different pass and reintroduces the defect). |
| OBJ-15 | CRITICAL (report-only scope) | CLAUDE.md:132-135 vs `overfit_check.py:80-98, 305-309, 745, 1060-1064` | The commit legislates OF-4 inertness and OF-5 deferral as things the reader must detect, and ships an instrument for neither: `info()` never moves PASS_N/FAIL_N, so there is no denominator. The shipped run is **7 of 13** (3 plateau INFOs + 2 monotone axes + DSR deferred), printed as "7 passed, 0 failed". And the new banner mis-attributes: OF-4's inertness is caused by a replay recording with `entries [0,0,0]`, not by row count. | Either produce the denominator on the summary line (`passed N, failed M, could-not-fire K`, K counted at `:305`/`:754`/`:1048`,`:1053` — **not** all 46 `info()` sites, which prints K=25 of mostly intended diagnostics), or state why the numerator is exempt from the same "what a reader takes away" argument that produced the corpus line. |
| OBJ-16 | CRITICAL | `test_windows.bat:57` vs `overfit_check.py:1125`; claim C2 | Machine reader unchanged: `return 0 if FAIL_N == 0 else 1` is byte-identical, `:57` is `\|\| (echo OVERFIT GATES FAILED - do not arm & exit /b 1)`, so a fully synthetic run still renders as `ALL GREEN.` at `:90`. Worse, CLAUDE.md now asserts permanently "**The corpus prints on the summary line — read it every run**" — false of `.\test_windows.bat`, the form CLAUDE.md itself mandates, with six stages between the banner and the takeaway. | Run `.\test_windows.bat` today on the DATA root and show a corpus qualifier adjacent to the final line. Do **not** contest by adding `--require-live-corpus` to `:57` — that reddens the DoD matrix until 640 new-era rows exist, which only accrue by running the bot, i.e. the release-condition-depends-on-the-thing-it-blocks shape CLAUDE.md's coordination notice names. `tests/test_battery_gate.py` (`_BAT` at `:23`) is the existing seam. |
| OBJ-8 | MAJOR | `overfit_check.py:1118-1120`; commit body "since era exclusion pushed the loaded corpus under the floor" | Wrong lever named. Era exclusion was armed and produced **2141 rows** on 2026-07-31 (`outputs/imported_sessions/cloud-mirror/overfit_report.md:1,3,16`, same 640 floor). The trigger is the era **reset**: `label_max_bars` 24→432 (7566ea88, 2026-08-01) minted `triple_barrier_h432`; first row 2026-08-12T20:56:18Z; 150th re-armed exclusion 2026-08-14T00:15:07Z. It will recur at every horizon migration. | Show a mechanism by which era exclusion alone drives a >640-row corpus below the floor. Then answer the consequence: `ml.era_exclusion.forced_off` (config.json:781, "restores the pre-exclusion training set exactly") is the un-fenced door to the same widening the new paragraph forbids by floors. |
| OBJ-13(a) | MAJOR | trial framing "WHAT IS ON TRIAL = the working-tree diff (`git diff HEAD`)" | The nominated artifact was empty at panel time; the change is commit f756676e. A review whose artifact is empty cannot fail — the same "check that cannot fail" class as its subject. **Docket-time update: it is no longer empty — it is an unreviewed `pyrightconfig.json +13` gate-config change.** | Nothing to prove — re-point the review at f756676e explicitly, and separately account for the pyrightconfig `include` list (adding it narrows bare-`pyright`/editor scope to the CLAUDE.md list; the explicit-path DoD command is unaffected, so state which surface was intended). |
| OBJ-3 | SERIOUS | CLAUDE.md:127-129; `overfit_check.py:247, 260-261, 1121` | `:247` has three conjuncts (`not force_synthetic and len(X) >= min_rows and 5 <= y.sum() <= len(y)-5`) but `:261` has one story; a class-balance trip prints the literally false `loaded rows=700 < 640` **as the new headline corpus line**. CLAUDE.md restates the row-floor leg as if it were the whole predicate. Degenerate labels are not hypothetical: era `exit_sim_time_stop` ran 3 positives / 459 rows in this very file. | Show that the class-balance leg cannot fire above 640 rows. Note the string also prescribes the wrong remedy ("wait for rows") for a labeler producing 0.65% positives. `grep -rn "loaded rows" tests/` = **0 hits**: nothing pins the reason text's truth. |
| OBJ-4 | MINOR | `overfit_check.py:1112-1120` vs claim C3 and CLAUDE.md:148-151 | The decaying number was relocated, not removed, in the same commit that declares the class defeated — and it decays against a *frozen* file: re-running the shipped loader at 18:28:29Z gave 352 while `signal_history.csv` was byte-identical (8,324,656 B / 10,659 lines, mtime 17:00:34Z), so drift is wall-clock label resolution, not file growth. | Contest only the narrowed form: `640` is freeze-pinned and written beside its expression (`len(FEATURE_NAMES)*10`), so it may stay; `346` must go or carry a minute-resolution stamp with "re-derive, do not cite". |
| OBJ-5 | MINOR | `overfit_check.py:254` → `:1121`; claim C2 | The live-branch string repeats the hazard class the file documents at `:256-259`; `n_live` is in scope and never printed. In a DRY_RUN-by-default system "live" collides twice (label source vs venue). | Overlaps OBJ-12; answer there. Additional fact the author must handle: `tests/test_preventive_maintenance.py:176` pins the literal `print(f"corpus: {source}")`, so any decomposition requires editing the pin in the same commit. |
| OBJ-6 | MINOR | CLAUDE.md:142-145 | Attribution only: `gate_truth_report.py:10-12` says "measurement standard, not a tunable" about **SG_MIN_ROWS** and nothing else (grep of that file: two hits, `:12` SG_MIN_ROWS and `:45` `_CONF_BUCKETS`; zero in `cohort_eval.py`/`cost_truth_report.py`). The commit message escalates it to "says EXACTLY that" about all three. | Cite the docstring text covering the overfit row floor and n=50, or restate each on its own authority (`cohort_eval.py:79 MIN_COHORT_N = 50`, `:125 ERA4_MIN_N = 50`; `overfit_check.py:180-189`). **Do not** adopt the objection's second half — the panel rejected "`--force-synthetic` bypasses the corpus check" as a false warning (`:247` short-circuits *into* synthetic and self-announces). |
| OBJ-7 | MINOR | commit body: "read 346, then 352, then 347 within the hour" | Fabricated provenance. Reconstructed loaded-count trajectory from the CSV in append order: in 15:22Z–16:22Z (the hour before the 16:22:53Z commit) the count changed exactly once, 348→349; 346 was last true 12:51Z, 347 at 13:02Z, and 352 was not reached until 16:40Z — after the commit. | Name the three runs (report path + header timestamp + cwd). "(moving)" itself is **not** contested — the count moved +39 on 08-15 alone (314@09:52Z → 353@17:00Z) — and the count is genuinely non-monotone (three measured decrements: 08-13T17:00:25Z, 08-14T13:48:42Z, 08-14T15:39:34Z, each −1 via the `ml/history.py:1573-1582` clash-dedup). Only the three-run sequence must be struck. |

## Withdrawn on cross-examination

The four objections withdrawn in full were not transmitted to this docket — only the 16 survivors were. What follows is every limb the panel **struck from a surviving objection** during cross-exam; each is a place the panel declined to pile on.

- **OBJ-13(b) — "the 32-line paragraph bisects the Definition-of-done paragraph" — WITHDRAWN.** `git show HEAD~1:CLAUDE.md | cat -A` proves the pre-commit state had "Every module must import in isolation…" hard-wrapped directly onto the command list with no blank line; post-commit it begins its own paragraph. The sentence became *more* separated, and it is grammatically self-contained (no anaphor). The claimed harm is inverted.
- **OBJ-6 leg B — "the overfit floor is materially weaker than 'not a tunable'" — REJECTED.** `config.json ml.overfit` holds only `dsr_n_trials`, so "never a config knob" is true under this repo's own definition; `--force-synthetic` short-circuits *into* the degraded branch and self-announces, so it can only make a gate read more degraded, never less; `min_rows` has zero overriding callers. Writing the proposed caveat into law would install a false warning.
- **OBJ-7 legs 1–3 — three supporting premises struck.** "A 352→347 decrease has no mechanism" is false (live-twin dedup + era filter nets −1; three real decrements measured). "The era subset determines the count" is false (`len(X)` is post-book-filter, post-dedup, post-dirty-drop, post-era; census 358 vs loaded 347 is a ~6-row gap the census cannot see, so the objection's own "354 at commit time" is wrong). "Volatility is the SOLE justification for the eviction" is false (the CLAUDE.md hunk carries its own class rule, and defect #2 names four other volatile numbers). Severity dropped SERIOUS → MINOR.
- **OBJ-2/OBJ-9's divergence story — struck, and replaced by a worse one.** All five spellings are byte-identical and cannot diverge *from each other*; the pin's docstring names a trigger (reword `reason`) that provably cannot fire. The real hazard is a **prefix rename at `:266`**, which flips all six consumers at once — including the diff's own new banner. Also: OBJ-9's "cannot fail" was narrowed to "cannot fail on the duplication already in the file" (a second *assignment* would trip it).
- **OBJ-8's second limb — "wrong onset date" and "falsifies directly" — struck.** Lines 1118-1120 make no onset claim ("every overfit green **this session**"); the onset defect lives only in the commit body, and "since era exclusion" reads as since-EVENT. And the cloud-mirror artifact is a 3-FAIL run, so it cannot falsify a claim quantified over greens — it falsifies the framing, not the claim. Severity BLOCKING → MAJOR.
- **OBJ-15's literal demand — corrected.** "K counts `info()` emissions" would print K=25 on the shipped run (46 `info(` sites vs 11 `check(`), dominated by by-design diagnostics — noise. Also OF-1's inclusion is inert in the measured state (`gate_is_informational` returns False when `on_synthetic`; all three `gap[*]` lines PASS). Two of three mechanisms, not three.
- **OBJ-16's headline demand — REFUSED.** Wiring `--require-live-corpus` into `test_windows.bat:57` reddens the DoD matrix until 640 new-era rows exist, and new-era rows accrue only by running the bot while shipping requires a green matrix — the exact "gate's release condition depends on the thing it blocks" shape CLAUDE.md's coordination notice records as this week's four-incident pattern.
- **OBJ-5's severity and framing — reduced.** The 2026-08-08 mislabel (`f"live rows={len(X)} < {min_rows}"`, `git show 3c0debd7`) was on the SYNTHETIC branch and is already fixed; `:254` shares the hazard class but never printed 467 and cannot (467 < 640 routes to synthetic). "ONE QUANTITY, TWO DERIVATIONS" is the wrong idiom — loaded and live are two populations. SERIOUS → MINOR.
- **OBJ-4's mechanism and half its demand — corrected.** The blamed cause (a mutating CSV) is wrong; and `640` survives (freeze-pinned, written beside its expression). SERIOUS → MINOR. It was not withdrawn because a false literal in a source file remains a false factual assertion.
- **OBJ-11's literal claim — conceded to the author.** "`gate_truth_report.py` has applied that standard since 2026-07-29" is **true**. The defect is enshrining as *exemplar* a tool currently issuing a verdict on a dead corpus.
- **OBJ-3's claim against CLAUDE.md:127-129 — narrowed.** The sentence is *incomplete*, not false: rows < floor ⇒ synthetic is a true sufficient condition. The defect is that it sits in an every-session file where it will be read as an iff, and silently omits `--force-synthetic` (live in CI at five call sites).
- **OBJ-1's headline — narrowed.** `.claude/workflows/README.md:72` records this session observing 346, 352 and 347 within one hour, so 346 was plausibly *measured*; "Measured 2026-08-15:" is honest as-of form. The surviving demand is provenance plus the eviction rule, not fabrication.

## The single objection most likely to be right

**OBJ-2** — and it is the one to answer first because it is the only objection I re-derived from the source inside this docket pass, and because answering it discharges three of the four BLOCKING rows.

`grep -n 'source.startswith("SYNTHETIC")' scripts/overfit_check.py` returns exactly five lines: `704` (the assignment), `833`, `1075`, `1084`, `1089`. `tests/test_preventive_maintenance.py:180-181` reads verbatim:

```python
    assert src.count('on_synthetic = source.startswith') == 1, \
        "exactly ONE place may derive on_synthetic; a second copy can diverge"
```

The assertion counts the assignment spelling; four bare re-derivations of the same predicate escape it, three of them filling parameters literally named `on_synthetic` (`:531`, `:429`, `:501`) from 350+ lines below a live in-scope variable. So the failure message is a false statement about the file it just read, and it ships green.

It is first because the consequence is not cosmetic. Per OBJ-10's point 4: `on_synthetic` feeds `gate_is_informational` at `:745` (`:101-118`, `return bool(explore_on) and not bool(on_synthetic)`), whose docstring states "an instrument must never grade itself leniently", and `config.json ml.exploration.enabled` is `true`. A rename of the `:266` prefix flips all five copies to False on planted-signal data, softening OF-1 and OF-7's dead-feature check to INFORMATIONAL, un-skipping `--epoch-ab`, and letting three diagnostics emit market-meaning text over the benchmark — while the battery prints "passed 7, failed 0" and the new pin stays green. OBJ-9 and OBJ-14 are the same defect seen from the test's side; OBJ-10 supplies the correct fragility direction. One fix — thread the single `on_synthetic` through `:833/:1075/:1084/:1089`, assert `code.count('source.startswith("SYNTHETIC")') == 1`, and pin the `:266` prefix (or return the bool from `load_dataset`) — closes all four. It is report-only tooling: no order, fill, fee, or geometry surface, so it does not trip the era-4 moratorium.

## What the panel could NOT assess

- **The four fully-withdrawn objections.** Their text was not transmitted with the surviving set. The "Withdrawn" section above is reconstructed from struck limbs inside survivors only; the author should not read it as the complete withdrawal record.
- **`pytest 3687 / smoke 219-0 / assurance 48-0`.** No artifact exists under the DATA root — `outputs/` matching `smoke|assur|overfit` yields `overfit_report.md` only. Only the overfit figure (7/0) is checkable, and its file's mtime precedes the commit. The `3686 → 3687` delta is merely *consistent* with the one added test function; nothing verifies the other two suites ran.
- **The claim quantified over greens (OBJ-8).** No real-corpus GREEN in the 2026-07-26 → 2026-08-14 window is on disk; `outputs/overfit_report.md` is untracked and `git ls-files` returns nothing for it. "Every overfit green since…" is untested, not refuted.
- **Whether `.\test_windows.bat` actually prints a corpus qualifier near `ALL GREEN.`** Establishing it requires executing the DoD matrix; a read-only pass can only read `:57` and `:90`. OBJ-16's falsifier is unrun.
- **The reconstructed loaded-row trajectory (OBJ-7).** The reconstructing panelist deliberately did not invoke the loader — `store_for_config` → `HistoryStore._ensure_schema` can rotate the production CSV (`ml/history.py:846,877`), a write to the DATA root. The ~6-row offset between the reconstruction (353) and the shipped loader's reading (347) is assumed constant; falsifying it requires running `overfit_check.py` against a scratchpad copy at a matched as-of row index. The hour-window argument does not depend on the offset; the ordering argument does.
- **The 2026-07-31 cloud-mirror run's `last_load_stats`.** OBJ-8's "era exclusion was armed and still left 2141 rows" rests on retention arithmetic (2141/2595 = 82% vs today's 347/358 = 97%), not a recovered stats dump. A dump showing `armed=false` would weaken the phrasing though not the conclusion (2141 > 640 either way).
- **The uncommitted `pyrightconfig.json` change now in the working tree.** No panelist saw it. Whether narrowing bare-`pyright` scope to the CLAUDE.md list is intended, and what it removes from editor/CI diagnostics for `scripts/` and `tests/`, is unassessed here.
- **Whether any post-commit session has cited an overfit green as strategy evidence** — OBJ-16's own falsifier for the change as a whole. It is a future observation; nothing on disk answers it.